# v9 実装検証（2026-09-05）

この記録は仮データを使った動作確認。キャラクター設定、本番素材、本編の返済バランスを確定するものではない。

## 行動一覧への改修後の確認

- `npm test`：55件を通過。経済・期限・セーブ移行のエンジンを変更していない。
- `npm run test:ui`：今回の画面構造に合わせて主要経路に更新。Chromiumの1280×720と390×844で4コマンドの初期表示・対象選択・戻る・人物・買い物・調合・納品・再読込を確認。667×375、800×360、844×390、932×430ではホームの4コマンドが最初から画面内で押せることを確認。
- 調合のダブルクリックで1回だけ作成されること、本日納品を残して1日使う際の警告とキャンセル、指定日納品を確認。計24の画面・経路チェックを通過。
- `npm run build`と公開用ビルドに対する`npm run test:touch`を通過。スマホのタップ操作で依頼→仕入れ→調合→納品を実行し、3日目・374G・体力38・薬湯在庫0を確認。縦横切替後も4コマンドを利用できる。
- スマホ横の主人公・3軸・行動一覧を目視確認。全ブラウザ・全画面・実機スマホでの再検証は今回の完了条件には含めていない。
- 過去の588画面検証・方針シミュレーションは以下の以前のUI再設計時の記録。今回再実行した検証とは区別する。

## 以前の自動検証

- `npm run build`：TypeScript型検査とVite本番ビルド。
- `npm test`：既存49件（UI再設計版は追加分を含め55件）。通常納品の繰返し、全件検証、在庫・体力・代償合算、選択順の不変性、同一人物の関係成長上限、固定日受付と指定日、前金の重複防止、解消・返還・停止、人物/場所/イベント/処方/依頼の解禁、人物依頼回数、章またぎ・最終日・保存往復・旧契約の回帰を含む。
- `npm run test:ui`：667×375、800×360、844×390、932×430、390×844、1440×900。通常選択の取り消し、特別受諾、前日納品の無効化、通常＋特別の同時納品、人物解禁、人物依頼の確認と実行、イベント途中の再読込、場所解禁と採集、掛け仕入れ、章末の支払割当、結末を確認。
- `npm run sim`：画面と同じエンジンで28日・84日を4方針ずつ実行。全行動後にセーブの読込往復を検証。特別依頼優先ではA/B両達成と人物・場所・イベント解禁をアサートする。

ブラウザの主要ボタンとページのはみ出し、実行時例外を検査。長い書類・選択一覧は内部スクロールし、納品確認・戻る・次へは常時操作可能にする。生成画像を目視し、地図の新着ラベルと狭い画面の行き先表示も確認する。

## 28日比較

| 方針 | 納品した依頼数 | 複数件の出発回数 | 章末後の資金 | 残債 | 特別達成 | 特別不履行 |
|---|---:|---:|---:|---:|---:|---:|
| 通常販売中心 | 10 | 0 | 251G | 9,250G | 0 | 0 |
| まとめ納品 | 11 | 5 | 926G | 9,250G | 0 | 0 |
| 特別依頼優先 | 10 | 4 | 590G | 9,250G | 2 | 0 |
| 人物依頼併用 | 8 | 4 | 0G | 9,398G | 0 | 0 |

## 84日比較

| 方針 | 納品した依頼数 | 複数件の出発回数 | 最終資金 | 残債 | 特別達成 | 特別不履行 |
|---|---:|---:|---:|---:|---:|---:|
| 通常販売中心 | 30 | 0 | 0G | 4,727G | 0 | 0 |
| まとめ納品 | 37 | 18 | 550G | 0G | 0 | 0 |
| 特別依頼優先 | 34 | 16 | 0G | 158G | 2 | 0 |
| 人物依頼併用 | 34 | 17 | 0G | 317G | 0 | 0 |

全方針で終了時の約束の未精算は0G。通常販売中心は1件ずつ、まとめ納品は最大2種類を準備する単純方針。特別依頼優先は完成品を確保し、指定日直前は休養して納品を優先する。人物併用は初期に帳簿・筆耕で処方と関係を開き、通常販売をまとめる。各方針は最適化探索ではなく、結果は選んだ準備方法にも依存する。

特別依頼優先では第1章の返済に496G不足し、第2章に繰越を含めて支払っている。最終的に158Gの残債が残る。新しい手段を得る準備と日々の販売との配分が結果に表れており、前金を純利益として数えていない。人物依頼併用も「人物依頼が弱い」という一般的な結論ではなく、序盤に処方を増やしてからまとめ納品する、この方針の測定値である。

今回、既存ノルマ（1050 / 1550 / 1950 / 2100 / 2450 / 2750G）は比較基準として維持した。追加された薬草園の素材・追加依頼をどの程度活用するか、特別依頼を何件置くか、待機時間が面白さにつながるかは人による試遊と次のバランス検討で確認する。

## UI全面再設計の検証

- エンジン・表示用計算は55件。既存49件を維持し、一括調合の個別操作との一致、数量不正・素材不足・体力不足の原子性、見込み計算の非破壊性、UI保存の検証、期限変更時の選択整理、準備メモの素材合算を追加。
- 28日・84日、4方針の比較結果は上記の表と一致。返済ノルマ・日末・期限・関係計算は変更していない。
- Chromium / Firefox / WebKit、667×375 / 800×360 / 844×390 / 932×430 / 390×844 / 1440×900で主要経路30画面ずつ、540画面を検証。PC・スマホ縦の追加境界48画面と合わせて588画面、失敗・実行時例外とも0。
- ページ全体のはみ出し、44px未満の操作領域、12px未満の文字、文字の横はみ出しを確認。各有効ボタンをスクロールして、実際のクリック判定に到達できることもPlaywrightで検証。
- 通常依頼から調合・仕入れ・依頼への往復と再読込で選択が残ること、一括調合、特別依頼の受諾、通常と特別の同時納品、人物・場所解禁、イベント途中の再読込、掛け仕入れの精算、章末、結末を確認。
- 追加の境界確認：在庫あり体力不足、空一覧、複数約束、人物依頼、長い記録、初期化の取り消し、保存失敗と再試行、縦横切替、Tabの循環・Escape・閉じた後のフォーカス復帰、二重クリック。
- 主人公画像は原本保持。生成した輪郭マスクを使ってRGBA化し、完全不透明な793,050ピクセルのRGBが原画像と一致。顔・白衣装・レースの確認点はalpha255、背景240,585ピクセルはalpha0。暗い背景とゲーム画面で目視確認。

実機の物理スマホを使った試遊と、初見の第三者による理解度・操作性の評価は、この開発環境では実施していない。ブラウザの自動操作・画面確認の結果を、その人間による最終評価の代わりにはしない。公開版での試遊が次の確認項目。

タッチの補足検証：`npm run test:touch`。Chromiumのタッチ有効・390×844から新規開始し、依頼選択→不足素材の追加→購入→2個調合→納品をtap操作で実行。結果は3日目・374G・体力38・薬湯在庫0。844×390へ切り替えて地図へ移動。物理端末の検証とは区別する。
