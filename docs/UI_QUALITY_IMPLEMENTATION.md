# UI品質改善の実装指示

2026-09-10。状態：**主要画面を改修済み**。[確認記録](UI_PRESENTATION_REVIEW.md)に今回の画像・確認範囲・残件を記載。以下は受入条件の全体であり、全状態が合格したという意味ではない。[品質管理基準](UI_QUALITY_STANDARD.md)に沿って、機能・保存と見た目の評価を区別する。

## 1. 今回の目標と制約

机・手紙・台帳の美術方針を活かし、追加した成長・選択・結果・回想を同じ作品のUIとして仕上げる。タイトルの欠けを直し、1200×500内の配置と実効文字・操作サイズを両立する。

尊厳ランク、回復、成長経験、依頼抽選、条件評価、選択順、報酬、保存形式はUI改善を理由に変えない。尊厳に応じてすべてのADVを書き分ける機能を追加しない。仮置きの人物・台本を本番へ書き直す必要はない。

共通部品と、確認を省力化する必要最小限の画像取得・測定を本作業に含む。見た目の仕上げを主な作業とし、操作テストや検証基盤作りに作業を偏らせない。機能試験は品質基準第6節で必要とした範囲だけ実行する。

## 2. 先に作る共通部分

まず変更の中心となる1画面を、品質基準V01〜V06の構図・素材・配色と文字・細部・場面の連続性・変化の表現に沿って仕上げる。その画面を共通部品の見本とする。既存UIの小文字や密度の問題まで引き継がない。

`src/DailyApp.tsx`の`c-button`、`src/ui/shell.tsx`の`button`、追加画面の生の`button`を確認し、現行の美術に合わせた共通操作部品へ集約する。対象は主操作、補助操作、アイコン操作、タブ、条件選択、一覧項目、状態行、通知、ダイアログ。

ボタンをすべて同じ形にする必要はない。用途ごとの違いを定義し、同じ用途で枠・余白・文字・フォーカス・無効状態を揃える。確認用の部品見本は開発専用に置く。エラー画面や回想の空状態も含め、通常のCSSを読み込んだ状態で確認する。

文字サイズ、色、間隔、操作領域、演出時間を共有の定義へまとめる。共有部品が従う値と実際の測定を結び付ける。新しいCSSを最後へ追記して偶然上書きする方法だけで仕上げない。変更した旧定義は適用箇所を調べて整理する。

## 3. 改善一覧と受入条件

| 作業ID・重さ | 再現・根拠 | 実装内容 | 合格の証拠 |
|---|---|---|---|
| UI-01 / P1 | タイトルを1280×720で表示。回想下部が切れ、設定がキャンバス下に隠れる | タイトル、説明、メニュー、通知の占有領域を設計し直す。回想入口をまとめる。検証入口は区別して残す | S01の全状態、2通常サイズと下限で全操作が見え、クリック・キーで到達。文字を縮めて解決していない |
| UI-02 / P2 | 成長下部の「選択の回想を開く」、ADV結果の「翌日へ」、新回想一覧が標準ボタンの外観 | 共通部品へ置換し、通常・フォーカス・無効・保存失敗も同じ様式にする | Q05の部品見本と全使用箇所の画像。結果と回想の動作が維持される |
| UI-03 / P1 | 844×390の「成長」は約23×24px、尊厳補足は約6.7px相当。390×844は全体が極小 | 小画面用のキャンバス内配置・論理サイズを設計し、下限未満は外側の向き／領域案内にする | Q01〜Q04測定、6サイズの表示、向き変更前後の保存位置一致。縦画面を縮小しただけで合格にしない |
| UI-04 / P2 | 成長4項目でも下部が隠れ、能力の比較にスクロールが必要 | 4項目の段階・経験・次までを一覧化。意味と育て方は個別詳細へ。回想入口は回想へまとめる | S04の0・直前・上昇・最大、最大文字でも4項目の主要値が同時に見える |
| UI-05 / P2 | 本文→条件付き選択で背景と立ち絵が消え、左右それぞれにスクロールが必要 | 直前の場面を継承した選択表示。必要な状態を短く比較できる領域、単一の選択一覧、条件／育て方の詳細を設計 | S06の3択・5択・8択・6条件。選択文・必要値・現在値を読め、詳細操作で選択を確定しない。本文→選択→本文の画像と通常速度確認 |
| UI-06 / P2 | 尊厳はランクと値の表示が中心で、意味や帯の境界を確認する入口がない | 三軸の概要と帯・不可逆性を1操作で確認。依頼の代償は名称との対応を明確にする | S03の全ランク境界・ランク0・同ランク回復。数値と表示が一致し、新しい上限パラメータを導入しない |
| UI-07 / P2 | 「回想」と「選択の回想」が別入口で、後者が成長画面の下にある | 共通の回想入口と分類へまとめる。場面回想と経路記録の保存は維持 | S08の0・1・50件と再生復帰。既存の解禁記録・選択履歴・本編に変更なし |
| UI-08 / 横断確認 | 部品、文字量、設定を含む全状態は新基準で未確認 | 結果・設定・確認・エラー・台帳を共通基準で確認し、発見した不具合に重さを付けて修正 | S07/S09/S10、複数変化の結果、保存失敗→再試行、全項目の見切れ確認 |

UI-06の説明例は「威厳 ランク3／数値45、帯41〜60、40以下でランク2、回復は60まで、下がったランクには戻らない」。これは表示案であり、新しい上限状態を保存する仕様ではない。

UI-05で、存在しない背景を台本ごとに追加必須にしない。現在の場面情報を引き継ぎ、未指定・読込失敗時の代替表示を用意する。特定のデバッグ台本IDだけに対応させない。

## 4. 実装の順序

1. **現行の欠けの再現と測定**：UI-01〜03の失敗をフィクスチャ化し、変更前画像を残す。測定は論理サイズと実効サイズを区別する。
2. **共通基盤と代表画面**：部品・文字・小画面方針を実装。タイトルと依頼画面で最低サイズを満たすことを先に確認する。ここで関連画面の退行を確認する。
3. **成長と尊厳の情報整理**：UI-04/06。名前、段階、値、説明をデータから描画し、主情報と詳細を分ける。
4. **ADVの一貫した見た目**：UI-05と結果部分。本文・選択・結果の画像を並べて仕上げる。遷移・操作も変更した場合だけ、関係する経路を1回確認する。外観だけの再調整では巡回を繰り返さない。
5. **回想・設定・異常時**：UI-07/08。入口、戻り先、通知を揃える。
6. **品質判定と配信確認**：対象基準を実測・目視し、残件を記録。公開も依頼されている場合だけ、検証済みビルドのPages反映まで確認する。

最初の段階で小画面の要件を確かめる。最後に全体を縮小して済ませない。途中のコミットは可能だが、部分修正を全UI完成として報告しない。

## 5. テスト追加の作業内容

追加する場合の`test:ui-quality`は、対象画面の直接再現、まとめた撮影、収容と実寸の測定を担当する。現時点では未実装。下記は基盤が扱う範囲であり、毎回すべてを試験する指示ではない。

- 画面状態を再現するデータと入口を開発・試験専用に用意。再現値は本編保存に書き込まず、試験用保存領域を使う。
- Q01〜Q04の測定と、Q08の外観状態を登録。Q07等の操作は既存試験を活用し、重複実装しない。必要な画面だけを選んで実行できるようにする。
- 画像名に画面・状態・サイズ・文字設定を含め、測定値と一緒に保存。成功時も画像を残す。
- 既存の`test:ui`/`test:adv-ui`を保つ。新しい案内仕様で期待する画面が変わる箇所は、ゲーム進行の保持と復帰の検証に更新し、サイズのケース自体を削除しない。
- 最低サイズ、最大文字、長文、最後の選択肢、回想末尾、設定復帰を検証。失敗時には画面と原因を保存する。

既存の全経済シミュレーションは表示変更だけなら繰り返さない。条件や保存へ手を入れた場合は対応する状態試験を追加する。テストの都合で本番UIの挙動を変えない。

画像を撮るための全ボタン巡回は禁止。既存の再現データを再利用する。成功した操作試験を手動でも繰り返さず、再実行には失敗・追加変更・具体的な懸念のいずれかを必要とする。改善一覧の動作に関する証拠は、変更に影響されない既存の試験結果を参照してよい。

## 6. 完了報告

[確認記録の書式](UI_REVIEW_TEMPLATE.md)で、UI-01〜08の修正状況、Q/S合否、未確認環境、残件を示す。合格の画像には表示サイズを添える。`build`成功や画面寸法だけを「商業品質」の根拠にしない。

この指示書の作成時点では、UI-01〜07は未解消、UI-08は未確認として扱う。証拠を伴う再確認でのみ状態を更新する。
