# ADV内の条件付き選択肢 — 実装指示書

| 項目 | 内容 |
|---|---|
| 文書ID | ADV-CHOICE-001 |
| 作成日 | 2026-09-09（日本時間） |
| 状態 | 設計・実装指示。初版保存時点では未実装。今回の更新は設計文書のみで、実装状況は再確認していない。 |
| 対象 | 成長パラメータの設計思想、ADV内の選択肢、条件判定、分岐結果、日次進行との接続、保存・回想 |
| 現行コードの確認基準 | `cbe3b5b0234b52bdf805fb4a1408926b4a035108` |

## 0. 本書の位置付け

本書は、ユーザーとの対話で合意した「ADV内の選択肢に条件を設ける」方針を、実装可能な単位へ整理したもの。

**第1章は対話で確認された方針と、今回追記する成長パラメータの設計思想をまとめる。具体例・設計候補はその旨を明記する。第2章以降は、その方針を実装するために本書で定める初期仕様・技術上の手順である。** 成長パラメータの最終構成や成長式など、まだ決めていない内容は第13章へ分離する。技術上の仮の型名・配置案・検証データを、完成版の確定コンテンツとして扱わない。

既存の依頼、主人公以外の人物、台本はシステム構築用の仮置きであり、本番の人物・依頼・物語を確定する根拠ではない。過去の対話で提案された人物名、章数、日数、エンディング案も、本書では採用を確定しない。交渉・知見・胆力・魅力は第1.4節で設計思想を記録するが、4項目を最終構成として固定したり、数値仕様が決まった扱いで本番へ追加したりしない。

`old/` の文書は現行仕様として参照しない。既存コード内の旧企画書参照コメントも、その企画を再採用する指示ではない。現在の挙動はコードとテストで確認し、今後の変更は本書の対象範囲に限定する。

## 1. 確認された設計方針

### 1.1 ゲーム体験

手紙で依頼を受け、仕事をしてお金を稼ぐ。その過程で主人公と依頼主の状況・関係が変わり、次の依頼や物語につながるADVを目指す。

今回の中心構造は次のとおり。

```text
依頼を選ぶ
  → ADVで事情を知る
  → 主人公の成長パラメータ・尊厳・関係によって選べる対応が変わる
  → プレイヤーが対応を選ぶ
  → 報酬・出来事・人物との関わりが変わる
  → 後日の依頼や物語が変わる
```

### 1.2 条件付き選択肢

1. ADV内に、成長パラメータ・尊厳パラメータ・人物別の関係値を条件とする選択肢を設ける。
2. **条件未達の選択肢も、その場面では表示する。** 選択文と必要条件を読み、何を満たせば選べるか理解できるようにする。
3. 条件未達の選択肢は実行できない。見えていることと選べることを区別する。
4. 条件が活動動機になるようにする。「この選択をするために成長パラメータを育てる」「この関係を築く」「この立場を保つ」と考えられることを目指す。
5. 条件付き選択の結果は、追加報酬だけでなく、出来事・依頼ルート・後日の手紙などにも接続できるようにする。
6. 成長パラメータなどの条件を満たさなければ到達できない物語があってよい。**成長条件が未達でも同じ物語・同じ結末へ到達できることは保証しない。**
7. 条件を満たすことは、その選択肢が実行可能になることを意味する。条件を満たした選択肢を自動で選ばせたり、常に最善の結末と扱ったりしない。

成長パラメータを単なる報酬倍率・体力節約・選択の助言だけのパラメータへ縮小しない。また、ADV内の条件表示を、手紙段階の依頼解放だけで代替しない。

### 1.3 成長パラメータを導入する目的

導入目的は、**依頼で得られる報酬を多様化すること**である。お金と人物別関係だけでなく、主人公自身に蓄積する成長を得られるようにし、その成長を後の選択・依頼ルート・物語へつなげる。

総称は「成長パラメータ」とする。技能という狭い範囲に限定せず、能力、世間への理解、精神的な強さ、人を引きつける力を含めて検討する。ただし、あらゆる選択条件を数値化する意味ではない。具体的な情報、約束、経験済みの出来事は物語フラグ等で区別し、**育てるパラメータをフラグだけで置き換えない。**

目指す循環は次のとおり。

```text
依頼・担当する役割を選ぶ
  → お金に加え、経験に対応した成長を得る
  → 以前は条件未達だった行動を選べるようになる
  → 任される役割・出来事・後続依頼が変わる
  → 次に何を育てたいか、誰と関わりたいかを選ぶ
```

成長パラメータは基本的に、条件を満たすか参照する値であり、選ぶたびに支払う通貨ではない。具体的な減少・維持・引継ぎのルールは別途決める。「今日の高収入」と「将来の選択を開く成長」のどちらを重視するか、依頼を選ぶ理由が変わることを重視する。

### 1.4 各成長パラメータの設計思想

以下の4項目は、今回の議論を受けて設計思想を記録する対象である。最終的な採用数・表示名・成長方式は未決。依頼や行動の例は役割の説明用であり、本番コンテンツの確定ではない。

| 項目 | 表すもの | 成長につながる経験の例 | 選択・物語での主な役割 |
|---|---|---|---|
| 交渉 | 利害を調整し、取り決めを作る力 | 仲介、売買、条件のすり合わせ | 契約の変更、代理交渉、第三者を含む取引への参加 |
| 知見 | 世間の仕組みや慣習を知り、事情を理解する力 | 調査、文書整理、異なる立場の人との仕事 | 問題の指摘、別の解決策の提示、複雑な事情への関与 |
| 胆力 | 圧力や緊張の中でも、自分の行動を貫く強さ | 難しい客への対応、大役、対立のある仕事 | 強い相手への異議、責任ある役目、危険を承知した提案 |
| 魅力 | 人の関心を引き、自分に関わりたいと思わせる力 | 接客、社交、人前に立つ仕事 | 指名・招待、協力の取り付け、主人公個人を目当てとした依頼への接続 |

#### 交渉

単なる報酬倍率ではなく、仕事内容・担当範囲・相互の責任を組み直せることに価値を置く。能力があれば相手を必ず言いくるめられるものではなく、相手にも受け入れる理由のある提案を可能にする。

通常の受諾に追加利益を付けるだけでなく、交渉の代理や仲介を引き受けることで、別の立場から物語へ関与できるようにする。条件を満たせば表示された選択を実行できるが、常に最大報酬・最善の結末を保証する意味ではない。

基本報酬の減額、前払い、追加支払などの契約変更は完成版に向けた設計対象であり、第2章・第7章の初期実装範囲を今回拡張する指示ではない。

#### 知見

依頼を経験して世間への理解を蓄え、それを使って出来事の意味を読み取れることに価値を置く。知識を表示するだけで終わらず、気づいた問題を指摘する、別案を出す、その調査を引き受ける、といった選択へつなげる。

万能の知識にはしない。例えば、手元の文書の不自然さを見つけることと、会ったことのない人物の秘密を知っていることは別である。特定の秘密・証拠は取得した情報の記録で管理し、知見の高さだけで自動取得しない。専門知識をどこまで共通の知見で扱えるかは、依頼設計と合わせて範囲を決める。

#### 胆力

仕事を通じて、圧力に向き合う強さが育つことに価値を置く。尊厳が低いことを、主人公の意志や行動力が弱いことと同一視しない。

威厳を失っても自分の意思で相手に向き合う、品位が低くても軽視される扱いに異議を唱えるなど、尊厳とは異なる軸で選択を作る。ただし、胆力だけで周囲の扱い・名声が回復するわけではない。

普通に断ることや基本的な意思表示まで、一律に胆力条件で禁止しない。公の場で強い相手に異議を唱えるなど、その場面で特に圧力へ立ち向かう必要がある行動に対応させる。「危険を承知で選べる」と「危険を受けずに必ず成功する」も区別する。

#### 魅力

容姿の点数ではなく、経験を通じて磨かれる話し方・存在感・人を引きつける振る舞いとして扱う。主人公個人が注目され、指名や誘い、協力を求める機会につながることに価値を置く。

威厳は貴族としての名声、人物別関係はその相手との積み重ねであり、魅力とは異なる。魅力が高くても、まだ信用されていない、対等には扱われていない状態が成立する。相手の意思を無条件に変えたり、全員の関係値を自動的に上げたりする力にはしない。

例えば、威厳が高ければ名家の人物として注目され、威厳が低くても魅力が高ければ家名から離れた個人として関心を持たれる、といった異なる展開を検討できる。

### 1.5 尊厳・人物別関係との分担

尊厳は成人向けシーンの方針を定める企画の根本である。今回確認した各軸の意味は次のとおり。

| 軸 | 低下するほど起きる変化 |
|---|---|
| 貞操 | 主人公が性に奔放になる |
| 品位 | 主人公が人として扱われなくなる |
| 威厳 | 貴族としての名声が失われていく |

**尊厳によって3択依頼の抽選対象が変わることを元の構想として尊重する。** 異なる依頼を通じて異なる場面へ進めれば、尊厳がシーンの方向を定める役割は成立する。同じADVに尊厳の全組合せの差分を作ることは必須にしない。

依頼の抽選条件と、ADV内の選択条件は別に設計する。尊厳をADV内の条件に使う機能は残すが、各場面で使うことを義務にしない。幅広い尊厳状態で同じ依頼を出す場合は、本文の前提が矛盾しないようにする。

成長パラメータは主人公自身に蓄積するもの、人物別関係は特定の相手との関わりの積み重ねとして扱う。成長条件を満たしていても、関係が必要な行動は別に条件を設けられる。ただし、すべての重要な選択を全パラメータの足切りにせず、その行動が可能になる理由に合う条件を使う。

尊厳が低下しても、成長が無意味になる設計にはしない。高い成長値と低い尊厳を組み合わせた先にも、その能力や強さを使う役割を検討する。成長値によって尊厳低下を一律に軽減する方式は、今回の設計思想から自動的に導入しない。

尊厳のランク制は検討中である。ランク数・段階の意味・変化条件・回復・内部の蓄積、および品位が誰からの扱いを表すかは未決。三軸の変化を自動連動させる規則も今回確定しない。

### 1.6 報酬・依頼・選択を設計するときの基準

- 依頼の職種名だけで成長先を決めず、実際にどんな経験・役割を引き受けたかと対応させる。すべての依頼に全項目の成長を付ける必要はない。
- 同じ依頼でも、ADVで担当する役割を選ぶことで成長先が変わる設計を検討する。例えば仲介なら交渉、調査なら知見、人前で難しい役目を担うなら胆力や魅力、という対応である。付与先・量・時点は本番データの設計で決める。
- 成長報酬は、その依頼や役割を選ぶ判断材料になるように提示する。手紙で示す範囲、ADVで示す範囲、経験量の具体的な表示は別途決め、未確定の成長方法を表示しない。
- 4項目がすべて「報酬が増える」「同じ結末へ別の台詞で進む」だけにならないよう、各項目を育てた先に異なる役割・出来事・後続依頼があるか確認する。
- 高い値は選択の実行資格を与えるもので、プレイヤーの選択そのものや最善の結果を決めるものではない。条件を満たした行動にも、物語上の責任や別の結果を持たせられる。
- 一度きりの場面の条件表示は主に次周の動機になる。今周の成長目標にしたい場合は、予告・保留・再到達の機会を別途設計する。後続依頼の抽選や優先配信の具体方式は未決。
- 最終構成を決める前に、各候補について「成長を得る経験 → 条件付き選択 → 選択後の違い」を一例ずつ設計し、育て方によって依頼選択や到達する物語が変わるか比較する。この確認はコンテンツ設計上の基準であり、第12章の機能テストを実行済みにするものではない。

## 2. 初期実装の範囲

### 2.1 実装するもの

- データ定義によるADVの選択・分岐・合流・終了。
- 成長パラメータ、三つの尊厳パラメータ、人物別関係値に対する条件判定。
- 必要値・現在値・未達理由を表示する選択UI。
- 選択結果による追加報酬、パラメータ変化、物語フラグの記録。
- 物語フラグを次の依頼の候補判定へ接続する最小機構。
- ADV途中と結果画面を復元できる保存。
- 未選択分岐を飛び越えないスキップ、進行を変更しない回想。
- 本番コンテンツと分離した検証シナリオ、および対象を絞ったテスト。

### 2.2 今回まとめて変更しないもの

成長パラメータの本番への追加・成長速度・全依頼の報酬、返済額、ゲームオーバー条件、章構成、人物設定、依頼の総数、画像制作、全体UIの再設計は対象外。

初期実装では、既存の基本報酬・消費・1日1行動の結果を保ち、その確定時点を分岐に対応できる形へ分離する。これは実装移行のための互換方針であり、完成版の日数や経済設計を再承認するものではない。

新しい会話エンジンや外部サービスを必須としない。既存の読書UI・画像表示を活かし、条件評価と進行の管理を分離する。

## 3. 現行コードとの接続点

以下は確認基準コミットにおける観測事実であり、完成版の仕様ではない。実装開始時に差分を再確認する。

| 場所 | 確認された構造 | この機能で必要な対応 |
|---|---|---|
| `src/daily.ts` / `dailyAction()` | 受諾時に報酬・体力・尊厳・関係・受注履歴・日付を更新し、表示用の場面を返す | 受諾、ADV進行、選択確定、依頼終了の責任を分離する |
| `src/DailyApp.tsx` / `commit()` | 更新後の状態を保存し、回想IDも記録してから演出とADVを表示する | 未確定の分岐結果を先に確定・解禁しない |
| `src/ui/scene.tsx` / `Dialogue` | `SceneLine[]` を順に読み、`onDone` で終了する。スキップは場面範囲を進める | 未決定の選択ノードで必ず停止し、選択を進行側へ通知する |
| `src/saveV14.ts` | v14の `DailyState` を保存・復元する。現在の型にはADVカーソルや選択履歴がない | 新しい保存形式と旧保存の移行を用意する |
| `src/gallery.ts` / `src/scenes.ts` | 回想IDはプレイ状態と別に保存し、目録の場面を再生する | 到達した分岐のみ記録し、回想で本編の状態を変更しない |
| `tests/daily.test.mjs` | `take` 直後の日付・報酬更新を含めて検証する | 新しい操作単位に合わせつつ、選択のない依頼の最終結果を保つ |

**読書画面に条件ボタンを追加するだけでは完了しない。** 特に、現在の `dailyAction()` を受諾時にそのまま呼び、その後で選択報酬を足す実装は避ける。判定対象が受注後の関係値・翌朝の回復値になる問題や、二重精算を起こすためである。

コード参照は第15章にまとめる。

## 4. 条件判定の仕様

### 4.1 値の参照

| 種類 | 参照先 | 注意 |
|---|---|---|
| 成長パラメータ | 登録された成長パラメータIDの現在値 | 成長パラメータ一覧・尺度は別データ。UIや判定関数へ名前を直書きしない |
| 尊厳 | `axes` の各軸の現在値 | 現行の `貞操`・`品位`・`威厳` を個別に扱う。合計値にしない |
| 関係 | 指定人物IDの現在値 | 場面の話者を暗黙の対象としない。依頼人以外を条件にしても対象名を明示する |

初期実装の「品位条件」は品位の現在値を参照し、`dignityCap` と混同しない。上限そのものを条件にする機能は今回追加しない。

### 4.2 比較と組合せ

- `min` は「以上」、`max` は「以下」。境界値を含む。
- 両方を指定した場合は閉区間の条件になる。
- 複数条件は `all`（すべて必要）または `any`（いずれか必要）で明示する。
- 条件なしは実行可能。空の `all` / `any`、上下限なしの比較、`min > max` は定義エラーとする。
- 尊厳などには「一定以上」だけでなく「一定以下」「範囲内」の条件も記述できる。高いほど必ず選択肢が増える判定に固定しない。
- 初期実装に確率判定・隠れた難度補正を入れない。表示した条件を満たせば、その選択は実行可能である。

### 4.3 判定時点

条件は、**その選択をする直前の論理状態**で判定する。第7章の作業中状態 `working` を使用する。

初回選択は受諾前の状態を基準とし、それ以前に確定した選択の効果があれば反映する。依頼終了時に与える基本報酬、関係上昇、成長のための経験、翌朝回復は先取りしない。

選択肢自身が与える成長パラメータ・関係上昇によって、その選択肢自身の必要条件を満たした扱いにしてはいけない。先の選択で状態が変わった場合、次の選択では新しい値で判定する。

表示時と実行時は同じ純粋関数を使う。実行要求の受信時にも最新のセッション状態とリビジョンで再判定する。

### 4.4 条件と代償は別

「交渉2以上が必要」は参照条件であり、選ぶと交渉を2消費する意味ではない。「関係2以上」「威厳60以上」も同じ。

値の消費・増加、報酬、品位上限の変更は、選択結果の効果として別に定義し、表示も分ける。

初期実装では選択による追加の金銭支出・追加体力消費は入れず、既存の基本消費を維持する。後で追加する場合は、支払可能性と参照条件を別に評価・表示し、負の所持金や体力を黙って0へ丸めて支払済みにしない。

### 4.5 不正なデータ

未登録の成長パラメータID・人物ID・軸、非有限値、範囲外の値、不正な型は「条件なし」と扱わない。定義時に検出し、実行時もエラーとして処理する。

新しく登録された成長パラメータについて旧保存に値がない場合だけ、移行処理で登録済みの初期値を補う。IDの誤記を値0で隠さない。

## 5. データ構造

本書の旧称に由来する `SkillId`、`SkillDefinition`、`kind: "skill"`、`skillDelta` は、以下の型案では広義の成長パラメータを表す。交渉だけでなく知見・胆力・魅力も同じ登録・条件評価の仕組みで扱える想定であり、職業技能への限定を意味しない。今回、コードや保存キーの改名・移行は行わない。

以下は責任範囲を示すTypeScriptの型案。名称や配置は変更可能だが、台詞文字列の解析、`eval`、UI内の場面ID別条件分岐で代替しない。

```ts
type SkillId = string;
type PersonId = string;
type NodeId = string;
type AxisId = "貞操" | "品位" | "威厳";

type ValueRef =
  | { kind: "skill"; id: SkillId }
  | { kind: "axis"; id: AxisId }
  | { kind: "relation"; personId: PersonId };

type Condition =
  | { kind: "range"; value: ValueRef; min?: number; max?: number }
  | { kind: "all"; items: Condition[] }
  | { kind: "any"; items: Condition[] };

// 必要な判定値と表示名・初期値・上下限はレジストリから取得する。
// ここで成長パラメータ名や0〜100という共通尺度を固定しない。
type SkillDefinition = {
  id: SkillId;
  label: string;
  initial: number;
  min: number;
  max: number;
  unit?: string;
  growthHint?: string;
};

type ChoiceEffects = {
  bonusMoney?: number; // 初期実装は0以上。基本報酬に加算する。
  axisDelta?: Partial<Record<AxisId, number>>;
  dignityCapDrop?: number; // 0以上。現在値低下と区別する。
  relationDelta?: Record<PersonId, number>;
  skillDelta?: Record<SkillId, number>; // 成長式そのものは別途設計。
  storyFlags?: Record<string, boolean>;
};

type AdvChoice = {
  id: string;
  text: string;
  condition?: Condition;
  hint?: string; // 条件を満たすための行動ヒント。隠れた条件に使わない。
  effects?: ChoiceEffects;
  next: NodeId;
};

type AdvNode =
  | { id: NodeId; kind: "text"; lines: SceneLine[]; next: NodeId }
  | { id: NodeId; kind: "choice"; prompt?: string; choices: AdvChoice[] }
  | { id: NodeId; kind: "end"; effects?: ChoiceEffects };

type AdvScenario = {
  id: string;
  version: number;
  entry: NodeId;
  nodes: Record<NodeId, AdvNode>;
};
```

`SceneLine` は既存の話者・本文・場面ID・画像指定を活かす。新しい型を導入する際、現行の `PersonId` などを不要に別定義へ置き換えず、参照解決層で接続する。

初期シナリオは循環なしのグラフとする。分岐後の合流は可能。IDの重複、存在しない遷移先、循環、終了へ到達しない経路を検証する。反復する仕事は新しいセッションとして開始し、同じグラフ内の無制限ループで表現しない。

### 5.1 条件評価の戻り値

`evaluateCondition(condition, working, registries)` は、可否の真偽値だけでなく、条件ツリーに対応した表示情報を返す。

最低限、対象の表示名、必要な下限・上限、現在値、各条件の達成状態、`all` / `any` の結合関係、定義エラーを持つ。UI用に別の判定を重複実装しない。

### 5.2 物語フラグ

選択結果は、表示文ではなく安定したIDで記録する。例：`test.route.negotiatorAccepted = true`。

後続依頼には `requiresStoryFlags` / `forbidsStoryFlags` に相当する条件を持たせ、既存の人物解放・回数制限などと**ANDで合成**する。フラグがあるだけで既存の受注不能理由を無視しない。

初期実装では、選択が立てたフラグによって、検証用の後続依頼が候補に入ることまで実装する。NPC全般の状態管理、依頼の優先配信・保留期限の体系は別途設計する。

条件付きの分岐を見せるための入口依頼を、その分岐と同じ成長パラメータ条件だけで閉じてはいけない。入口と分岐は別の判定である。そもそも別ルートに属する未訪問場面まで一覧公開する必要はない。

## 6. 選択肢UI

### 6.1 基本表示

到達した選択ノードでは、定義された選択肢を定義順で表示する。条件未達でも選択文を `???` に置き換えたり、非表示にしたりしない。初期実装に条件不足による非表示モードは設けない。

表示例は次のとおり。名称・数値は検証用であり、成長パラメータの採用案ではない。

```text
取引条件の交渉を任せてほしいと申し出る
  条件未達：交渉 2以上 ／ 現在 1
  選択した場合：追加報酬 100G

私的な相談として、詳しい事情を任せてもらう
  選択可能：依頼主Aとの関係 2以上 ／ 現在 2

今回は追加の話を引き受けない
  条件なし
```

複数条件は省略せず示す。「交渉2以上 **かつ** 関係2以上」と「交渉2以上 **または** 関係2以上」を区別する。ORの一方が未達でも、もう一方で選べることが分かる表示にする。

下限は「不足」、上限は「上回っている」など、比較方向に合う説明を出す。関係値には対象人物名を必ず付ける。

### 6.2 操作

- 条件未達の選択肢は読めるが、タップ・クリック・Enter・Spaceのいずれでも実行できない。
- 色だけで可否を表現しない。ロック状態、必要値、現在値を文字で示す。
- キーボードでもロック理由を確認できるようにする。例えば `aria-disabled` と説明テキストを用い、イベント側と進行側の両方で実行を止める。
- 選択表示中は背景タップによる本文送りと衝突させない。直前の文字送りキーの押しっぱなしで最初の選択肢を確定しない。
- ログ・設定・絵だけ表示から戻っても、選択位置と内容を保持する。ログの過去の選択を押して、本編を巻き戻さない。
- 二重クリックや複数入力を一回の選択として処理する。

### 6.3 画面と情報量

[AGENTS.md](../AGENTS.md) の1200×500固定キャンバスを維持する。選択時だけ専用の領域を表示してよいが、縦持ち専用レイアウトや全体画面の作り直しは行わない。

長い選択文・複数条件は折返しと選択領域内のスクロールで処理する。画面内へ押し込むために本文や条件を過度に縮小しない。ホバーだけで条件が分かる作りにしない。

既存の文字サイズ・動きを減らす設定を尊重する。条件の詳細と現在値は確認できるようにする一方、数式や内部フラグを通常画面へ露出させない。

### 6.4 条件と未来の見せ方

必要条件と、その場で約束される報酬・明示的な代償は隠さない。ただし、将来の場面名・結末・相手の秘密をすべて予告する必要はない。

`hint` は何を育てる・誰と関わるとよいかの説明に使う。成長方法自体が未設計なら「この仕事で育つ」と本番向けに捏造しない。

一度きりの場面で初めて条件を知る場合は、主に次周の動機になる。今周で再挑戦させたい依頼は、予告や返事の保留などをコンテンツ側で明示する。今回、全選択の再挑戦・無償取消は実装しない。

## 7. 受諾から依頼終了までの状態遷移

### 7.1 基本方針

受諾しただけで、未来の分岐結果や翌朝の回復まで本編へ反映しない。未完了の依頼を `activeSession` として保存する。

```text
机上
  → beginJob：受諾・セッション保存
  → ADV本文
  → 選択待ち
  → choose：条件再判定・選択記録・作業中状態の更新・保存
  → 分岐先の本文／次の選択
  → end：依頼の結果と日次進行を一度だけ確定・保存
  → 結果画面
  → acknowledgeResult：結果を閉じるだけ
  → 翌朝または章末精算
```

初期実装の依頼は一回の終了で一日進む。選択肢を一つ押すたびに日付を進めない。

### 7.2 セッションで保存する情報

- `sessionId`：同じ依頼の再受注でも新しくなる実行ID。
- `runId`、依頼ID、シナリオIDと版、受諾した章・日、セッションのリビジョン。
- `entrySnapshot`：受諾前の状態。循環参照を避け、セッション自身は含めない。
- `quote`：受諾時の基本報酬・体力消費・固定代償など。現行の割引や材料費を含む計算結果を固定する。
- `working`：受諾前の値へ、それまでに確定した選択効果を順序どおり適用した作業中状態。
- 現在のノードID、本文の行IDまたは行位置、文字位置。
- 選択済みノードと選択肢IDの履歴、適用した効果、到達した場面ID。
- `phase`：本文・選択待ち・結果表示などを区別する状態。

同じ本文を画面サイズに応じて改ページし直しても、選択履歴やノード位置は変わらないようにする。成長パラメータ・尊厳・関係の条件欄は `working` の値を表示する。

### 7.3 受諾

受注資格、体力、進行状態を確認し、`quote` とセッションを保存する。基本報酬、基本の関係上昇、受注回数、日付はまだ本編へ適用しない。

セッション中は他の依頼、休養、章末精算を実行できない。タイトルへ戻る・再読込は中断保存であり、受諾取消ではない。復帰時はこのセッションを再開する。

### 7.4 選択

進行側は `(sessionId, nodeId, choiceId, expectedRevision)` を受け取り、現在の選択ノードに属する選択肢か、条件を満たすか、未処理かを検証する。UIから効果や遷移先を直接受け取らない。

有効なら、選択効果を `working` へ一度だけ反映し、履歴と遷移先を同じ保存に書く。保存が成功した後に、次の画面へ進む。

無効な要求は状態を変更せず、理由を返す。UIを経由しない呼出しでも、条件未達の選択は実行できない。

### 7.5 依頼終了と一度だけの精算

終端ノードに到達したら、次の順序で一つの保存結果を作る。

1. それまでの選択効果を反映済みの `working` を引き継ぐ。選択効果をもう一度適用しない。
2. 終端に定義された効果を一度だけ適用する。
3. `quote` の基本報酬・基本消費・固定代償・基本の関係上昇など、未適用の依頼効果を適用する。
4. 受注回数・人物解放・依頼履歴を更新し、既存の日次回復と日送りを一回実行する。威厳をその日に削ったかの判定には、基本代償だけでなく選択の効果も含める。
5. 完了済みの結果データ、差分内訳、選択履歴を保存し、`phase = result` とする。

初期実装では基本報酬を固定し、選択ごとの `bonusMoney` を加える。結果画面は基本報酬と追加分を分けて表示する。選択後の関係値から基本報酬を再計算しない。基本報酬の減額、前払い契約、追加の金銭・体力支払は別途設計する。

パラメータの上下限と品位上限低下は共通処理に集約し、プレビューと確定で一致させる。品位低下を選択効果と既存の上限低下関数で二重適用しない。

`acknowledgeResult` は結果表示を閉じるだけで、報酬・日付・関係を更新しない。結果画面の再読込からも二重精算しない。

### 7.6 選択のない既存依頼

既存の直線的な台本は、本文ノードと終端ノードへ変換して同じ流れで再生できるようにする。台本を書き直すことや全依頼へ仮の選択肢を付けることを必須にしない。

完了後のお金・体力・尊厳・関係・日付・返済状態は、同じ初期状態で旧処理を一回実行した結果と一致させる。変えるのは確定タイミングと途中保存であり、既存全体の収支ではない。

## 8. 保存・復元・例外処理

### 8.1 形式と移行

保存形式を新しい版へ更新する。次の版番号とキーは実装開始時の最新版から決め、v14の解析器へ新しい構造を曖昧に混在させない。

旧保存の所持金、残債、日付、三軸、品位上限、関係、受注履歴などは保持する。成長パラメータは登録された初期値、物語フラグは未達状態、セッションはなしで補う。新保存を読めない場合に旧保存へ黙って巻き戻さない。

**v14の保存は、ADVの再生前でも報酬・日付の更新が済んでいる場合がある。** 旧保存には再生位置がないため、移行時に直前の仕事を再受諾・再精算しない。保存されている更新済みの状態から再開する。

元の旧保存を削除しない。UI設定と既存回想も維持する。未採用の成長のための経験を過去の受注履歴から推測して付与しない。

### 8.2 途中復元

本文途中、条件付き選択待ち、選択直後、終了処理後、結果画面の各段階で復元できること。再読込によって現在値が受諾前や翌朝へ誤って切り替わらないこと。

セッションと日次状態は一つの論理トランザクションとして保存する。分岐記録だけ・報酬だけを別々に確定しない。

保存失敗時は未保存の次の画面へ進まず、直前の確定状態を保持して再試行を案内する。再試行で同じ効果を再加算しない。

### 8.3 更新後のシナリオ不一致

保存が参照するシナリオ版・ノード・選択肢が存在しない場合、先頭から自動再生したり成功扱いにしたりしない。

シナリオ版を保持して再開するか、明示的な移行対応を行う。初期実装で対応不能な未完了セッションは、理由を表示し、ユーザーの確認で `entrySnapshot` へ戻す救済を用意してよい。完了済み結果をこの救済で巻き戻してはいけない。元の保存はバックアップする。

## 9. スキップ・ログ・回想

### 9.1 本編のスキップ

通常スキップ・既読スキップは本文だけを送る。**未決定の選択ノードでは必ず停止する。** 以前の周回で同じ場面を読んでいても、自動選択しない。

選択待ちで `onDone` を呼んで依頼終了へ飛ばさない。ログや設定を開閉しても選択は確定しない。

### 9.2 到達記録

未選択の枝の場面を回想へ登録しない。受諾時にシナリオ内の全場面IDを一括で解禁しない。

場面IDは必要に応じて枝ごとに分け、既読判定も未訪問の枝へ波及させない。選択文を見ただけで分岐先が既読になることはない。

### 9.3 回想

回想はそのプレイで到達した枝、または保存された選択経路を読み取り専用で再生する。現在の本編パラメータで過去の選択を再判定しない。

回想の再生・終了・スキップによって、本編の所持金、成長パラメータ、尊厳、関係、物語フラグ、日付を変更しない。既存回想の再生を維持する。

回想保存は本編と別領域でもよいが、同期失敗時は本編の確定記録から到達IDを冪等に再同期できるようにする。本編の報酬を再実行して回想を復旧しない。

## 10. 進行不能と条件設計の区別

初期実装の各選択ノードには、条件に左右されず進める選択肢を最低一つ用意する。例えば「追加の話は断る」「今回は通常の仕事だけにする」などで、通常終了へ到達できるようにする。

**これは操作不能を防ぐ技術上の初期規則であり、高条件の物語を低条件でも見せる救済規則ではない。** 条件なしの選択から、条件付き選択と同じ依頼ルートへ自動的に合流させる必要はない。

すべての条件付き選択を上位互換にする必要はない。関係を深める道、高報酬の仕事へ進む道、現在の立場を使う道など、異なる結果を作れるようにする。ただし、全ルートの価値配分・最終結末は本書では確定しない。

条件は行為や役割の成立理由に対応させる。関係が足りないから「助けたいと思う」ことができない、といった内心の禁止より、「内情を任せてもらうには関係が必要」という表現を使う。

ゲームオーバー条件の追加や変更は今回行わない。技術的な袋小路を、意図した物語上の失敗として扱わない。

## 11. 最小検証シナリオ

検証専用の依頼主Aと成長パラメータ `test_negotiation` を使う。本番の成長パラメータ一覧・人物一覧へ採用する指示ではない。テスト／開発用データに隔離し、通常の依頼抽選へ混入させない。

### 11.1 初期状態と選択

基本報酬200G、基本体力消費20、尊厳の固定代償なし、成長パラメータの仮の尺度0〜3とする。値は機能検証用。

共通の仕事場面の後に、次の選択ノードを置く。

| ID | 選択の内容 | 条件 | 検証する結果 |
|---|---|---|---|
| `normal` | 追加の話は引き受けない | なし | 基本報酬のみ。専用の後続依頼は開かない |
| `negotiate` | 代理交渉を引き受ける | 検証用交渉2以上 | 追加100G、別の本文、`test.route.negotiatorAccepted` を立てる |
| `introduce` | 正式な紹介役を引き受ける | 威厳60以上 | 別の本文と別の後続フラグ |
| `confide` | 私的な協力者として任せてもらう | 依頼主Aとの関係2以上 | 関係を軸にした別の本文・後続フラグ |
| `alternative` | 今の立場に応じた別の役割を引き受ける | 威厳30以下 | 上限条件が機能する枝 |

別の小さな検証ノードでAND・OR・範囲条件を確認する。UIを見やすくするために、本番場面へすべての条件形式を同時に詰め込まない。

### 11.2 活動動機までつなぐ検証

同じ選択場面について、検証用交渉1の状態では条件文が表示され、代理交渉は選べないことを確認する。

検証用の経験獲得依頼を完了して交渉を2にし、もう一度到達可能な検証シナリオで、その選択が有効になることを確認する。これは成長・再挑戦システムの本番仕様ではなく、条件表示から値の変化までの接続テストである。

`negotiate` を選んだプレイだけで、完了後に検証用の後続依頼が候補へ入ることを確認する。別の選択でも同じフラグを立てて、分岐の差を消してはいけない。

## 12. 実装順序と受入条件

### 12.1 作業順序

1. 最新の `AGENTS.md`、対象コード、テストを再確認する。第3章の基準との差分を記録する。
2. 条件型、パラメータ参照レジストリ、純粋な条件評価、定義検証を作る。
3. シナリオのノード進行、選択処理、物語フラグをUIから独立して作る。
4. 受諾・進行・完了を分離し、セッション保存と旧保存移行を接続する。
5. 既存の読書UIへ選択表示を追加し、スキップと回想を対応させる。
6. 検証シナリオを接続し、後続依頼までの縦の流れを確認する。
7. 変更対象に絞って回帰確認し、実装済み範囲と未完了範囲を本書の状態欄へ追記する。

配置案は `src/adv/conditions.ts`、`src/adv/types.ts`、`src/adv/session.ts`、`src/ui/AdvChoices.tsx`。ファイルを増やすこと自体は要件ではないが、条件評価・状態更新・表示の責任は分離する。`src/` 全体の大規模な再配置は不要。

### 12.2 受入テスト

| ID | 確認内容 | 合格条件 |
|---|---|---|
| A01 | 三種類の値 | 成長パラメータ・各尊厳軸・指定人物との関係を正しい参照先で判定する |
| A02 | 境界 | 下限直前・下限一致・上限一致・上限超過、範囲内外で期待どおりになる |
| A03 | 複合 | AND・ORの結果と、各条件の表示が一致する |
| A04 | 不正データ | 不明ID・NaN・空条件・不正範囲・欠損遷移・循環を検出する |
| A05 | 未達表示 | 選択文、必要値、現在値、未達理由を読めるが実行できない |
| A06 | 不正操作 | 無効な選択ID、別ノードの選択、古いリビジョン、条件未達の直接呼出しで状態が変わらない |
| A07 | 時点 | 終了後の関係上昇や翌朝回復を先取りしない。先の選択効果は次の判定に反映する |
| A08 | 分岐 | 選択に応じて本文・追加報酬・フラグ・後続依頼が異なる |
| A09 | 一度だけ | 連打、保存再試行、再読込、結果の再表示で報酬・関係・日付が重複しない |
| A10 | セッション | 本文・選択待ち・選択直後・結果画面を復元し、途中で他の依頼を受けられない |
| A11 | スキップ | 通常・既読のいずれも未決定の選択で止まる |
| A12 | 回想 | 到達した枝だけを記録し、再生しても本編の値・フラグが変わらない |
| A13 | 互換 | 旧保存を壊さず移行する。更新済みの旧報酬を再付与しない |
| A14 | 既存依頼 | 選択なしの依頼を終了した状態が、旧処理の最終結果と一致する |
| A15 | 章末 | 最終日も依頼終了後にだけ精算へ進み、途中再開で章を飛ばさない |
| A16 | UI | 固定キャンバスで長文・複数条件を確認でき、キーボード・タップ・設定の開閉が衝突しない |
| A17 | 不足時の出口 | 条件不足でも通常終了へ進める。ただしロックされた物語ルートは開かない |
| A18 | 成長との接続 | 検証用経験の獲得後に、以前ロックされていた選択が有効になる |

`npm run build` と変更範囲の自動テストを実行する。現行 `npm test` は `scripts/run.mjs` で対象を明示しているため、新しいテストファイルを置いただけで実行対象になったと思い込まない。テストランナーへの登録も確認する。

既存のUI検証を無条件に全種類・全サイズ繰り返す必要はない。新しい選択画面の代表サイズ、条件の多い場面、保存・進行の境界を優先する。体裁だけでなく、A08・A09・A10の機能確認を完了条件に含める。

**ドキュメント作成時点では、これらの実装テストは未実行。** 本書の存在を実装完了や公開完了の証拠にしない。

## 13. 別途決める事項

以下は今回の実装で勝手に確定しない。

- 交渉・知見・胆力・魅力の最終採用構成、表示名、ID、初期値、上限、尺度・ランク数、成長式・成長量、経験を得る本番依頼、減少・維持・周回引継ぎ。
- 成長報酬の提示方法と、依頼単位・選択単位の付与先・付与量・付与時点。条件判定と効果確定の順序は第4章・第7章に従う。
- 各選択肢の本番条件値。尊厳三軸の意味は第1.5節を前提とし、ランク制の採否、段階・変化・回復・内部蓄積、品位が誰からの扱いを表すか、三軸間の連動は別途決める。
- 人物と依頼の本番内容、分岐数、報酬、結末。
- 返事の保留、再挑戦、重要依頼の予告・優先配信。
- 基本報酬を変更する契約、前金、追加の金銭・体力消費。
- 借金・期限・ゲームオーバー・全体の経済バランス。

これらが未確定でも、条件評価・表示・分岐・保存は検証用データで実装できる。完成版へのコンテンツ展開は、別途合意したデータを差し込んで行う。

## 14. 実装担当者への要約

> 依頼報酬を多様化するため、技能に限定しない、育てられる成長パラメータを設ける。
> 交渉・知見・胆力・魅力の設計思想は第1.4節を参照し、最終構成と数値は別途決める。
> ADV内の選択肢に、成長パラメータ・尊厳・人物別関係の条件を設ける。
> 尊厳による依頼抽選とADV内の条件を分け、全ADVへの尊厳別差分を必須にしない。
> 未達でも選択文・必要値・現在値を示し、活動の目標にする。
> 選択結果を報酬だけでなく、物語と後続依頼へ接続する。
> 成長条件が未達でも同じ物語を見せることを必須にしない。
> 条件判定と値の消費を混同せず、受諾時に未確定の結果を先取りしない。
> 成長パラメータ一覧・人物・バランスは仮例から確定せず、まず一つの検証シナリオを最後まで動かす。

## 15. 参照と更新記録

初版が記録した挙動については、すべて確認基準コミット `cbe3b5b0234b52bdf805fb4a1408926b4a035108` の以下のファイルを確認した。通常の相対リンクは今後の更新に追随するため、過去時点を調べる際は当該コミットを指定する。

- [作業方針](../AGENTS.md)：旧資料の扱い、画面比率、検証範囲。
- [日次処理](../src/daily.ts)：`DailyState`、`dailyAction`、`offersOf`、`payOf`。
- [アプリ側の進行](../src/DailyApp.tsx)：`commit`、`persist`、`Dialogue` の呼出し。
- [読書UI](../src/ui/scene.tsx)：`Dialogue`、`tap`、`skipCurrent`。
- [保存](../src/saveV14.ts)：`parseDaily`、`loadDaily`、`saveDaily`。
- [回想保存](../src/gallery.ts) と [場面目録](../src/scenes.ts)。
- [日次テスト](../tests/daily.test.mjs) と [実行コマンド](../package.json)。

| 日付 | 変更 |
|---|---|
| 2026-09-09 | 初版。条件付き選択の方針、初期実装範囲、状態遷移、保存互換、検証手順を文書化。ゲームコードは変更していない。 |
| 2026-09-09 | 設計議論を反映。技能から広義の成長パラメータへ整理し、交渉・知見・胆力・魅力の設計思想、報酬としての役割、尊厳三軸の意味、依頼抽選との分担、未決事項を追記。文書のみ更新し、コード・保存形式・実装テストは変更・実行していない。 |
