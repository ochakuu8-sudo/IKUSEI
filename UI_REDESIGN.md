# スタミナ制・3コマンド構成（v10、2026-09-05）

この節が現行仕様。過去の「行動で1日消費」「薬以外の常設仕事」「旧契約だけ2日消費」の記述を置き換える。

- ホームは依頼／収集／調合。持ち物・設定と「一日を終える」は補助操作。約束帳はHUDの日付から開く。
- 依頼は通常薬納品・特別依頼のみ。詳細で不足品を確認し、準備が必要なら下部の主操作から収集か調合へ進む。数量入力や購入・採集・調合を依頼詳細に同居させない。
- 収集は採集／仕入れ。準備中の薬と必要数を引き継ぎ、素材が揃ったら調合へ進める。調合後は元の依頼詳細へ戻る。通常行動は同じ画面に残り、短い結果通知を表示する。
- 採集・調合・納品は従来のスタミナ量を消費するが日付を進めない。仕入れは資金のみ。契約受諾・支払・解消・延長は日付もスタミナも消費しない。資金・受付・回数条件は引き続き検証する。
- 新規開始はスタミナ100。一日を終えると期限を判定し、翌朝100へ回復する。0でも自動で終了せず、余っていても終了できる。14日目は精算を挟み、次章開始時に100へ回復する。
- 日末に品位＋6（上限以内）。当日に威厳を下げる行動がなければ威厳＋2。貞操は回復しない。日単位の取引履歴も日末に一度だけ更新。
- 通常薬依頼は同日でも繰り返せる。通常納品の関係上昇は人物ごとに一日＋1まで、特別報酬は別。まとめ納品は資源・代償を合算し、全件を実行前の状態で検証する。
- 特別依頼の指定日、前金、残額、返還義務は維持。指定日の途中で他の行動をしても不履行にならず、その日を終えた時だけ判定する。
- 薬以外の常設仕事・親交訪問・休憩は実行不可。ヴェルネ／マルク／解禁後の薬師に仮の薬湯依頼（2個、330G、スタミナ12）を追加。香油はクレール関係2で習得し、他の関係処方も薬の取引から到達できる。
- セーブはv10。v7／v8／v9から資金・在庫・関係・契約・日付・残存スタミナ・履歴・解禁・未読イベントを維持し、当日記録だけ空で開始。旧契約の納品日数は無効化し、2日だった代替品もスタミナだけでまとめ納品可能。数量・期限・金額・報酬・延長回数のスナップショットは維持する。
- 旧セーブで既に関係条件を満たした未習得処方は、その人物への次回納品で取得。読込だけでは解禁しない。

## 以前の設計記録

# 今日の行動から選ぶUI（2026-09-05）

この節が現行の操作仕様。下の監査表は最初の再設計時の記録です。

- 自室の主行動は「仕事をする／出かける／休む」。調合・持ち物・設定を小さく配置。選ぶだけは0日、確定時に実日数を示す。状態行は具体的な薬・相手・回復量の誘い文。
- 仕事一覧に薬・人物依頼・特別依頼を統合。種別件数、人物フィルター、紹介を失った既知の仕事と理由を表示。未登場人物や未習得処方は先取り表示しない。納品チェックは常設。
- PCは一覧と詳細、スマホ縦は下から開く詳細シート。依頼の不足薬・素材から仕入れ／採集を直接確認し、その場で数量指定の0日調合ができる。準備後は依頼詳細とフィルターへ戻る。
- 外出は人物・採集・買い物を直接選ぶ。広い画面は地図上、狭い画面は同じ対象を一覧化し、切替タブは設けない。自宅は外出先に含めない。人物の依頼は統合一覧へのフィルター。
- 日数を使う行動の結果画面は「確認」ひとつ。通常は自室、準備の購入／採集は元の詳細へ戻り、章末・結末を優先。旧契約の2日納品は2日と表示する。
- ActionDockで戻る・見込み・確定を揃え、期限警告を確定ボタンの上に置く。スマホでは下部固定。空の約束帳はホーム主導線から外し、HUD日付と本日の札から開く。
- 全画面CGの上にウィンドウなしの短い文字を表示。画面全体のタップで全文表示／次送り。長押し・上スワイプ・小さな「⋯」から会話ログ／非表示を開く。メニュー操作はセリフを進めない。長文のスクロールも送りと分離。

ゲーム経済・日数計算・セーブv9は変更しない。表示見込みは共通エンジンの非破壊実行から取得し、UI専用の見た仕事・選択・設定は ikusei-ui-v1 に保存する。過去のUI項目は読込互換性のため残すが、新しい準備往復は明示的な戻り先を使う。

以下は最初のUI全面再設計時の監査記録。

# UI全面再設計 — 令嬢の薬房と帳面

## 監査と対応方針

| 画面・状態 | 監査した問題 | 今回の設計 |
|---|---|---|
| タイトル／保存なし・あり | 絵がなく、試作開始が主操作と混在 | 絵と作品名、続き・新規・設定、試作は設定内 |
| HUD／縦・横 | 残債が主役、極小操作 | 日付・資金・体力、返済までの日数と不足額 |
| 自室／期限・新着 | 立ち絵上の3軸がコマンドに隠れる | 絵・3軸・今日の予定を独立した領域に配置 |
| 依頼書／1件・空・不足 | 横長カード、条件長文、選択消失 | 最大幅付きカード、比較項目、フィルター、永続選択 |
| 特別依頼／受付・準備・当日 | 通常と見分けにくい | 封蝋、固定日程、構造化した条件 |
| まとめ納品／競合在庫・期限変更 | 長い確認、準備への導線なし | エンジン見込み、素材への往復、選択再検証 |
| 調合／未知・不足・複数 | 1個ずつ、入手先不明 | 処方と詳細、数量、必要数、入手先 |
| 約束帳／多数・完了・返還 | 1枚ずつ送る操作 | 期限順一覧、詳細、14日予定表 |
| 地図／新人物・未解禁 | 背景不在、訪問しないと内容不明 | 地図と一覧、素材・頼まれごと・新着 |
| 人物／回数制限・不足 | 人物と仕事が混在 | 人物選択、関係・依頼・支援を分離 |
| 採集・仕入れ／空籠・資金不足 | 小さい増減、合計が遠い | 素材アイコン、数量入力、確認で合計・日付 |
| 会話・イベント／長文・再読込 | 画面全体が送りボタン | 独立した会話枠、全文表示・ログ・設定 |
| 結果／複数人物・解禁 | 最後の関係のみ、金額の反復 | エンジン差分から全人物、通常は通知、詳細を展開 |
| 章末・結末／不足・支払 | 見込みなのに確定後の表現 | 精算前後を区別、約束支払い導線、実績 |
| 確認・設定／保存エラー | フォーカス制御なし、即初期化 | 共通ダイアログ、明示確定、近傍エラー |

## 実寸レイアウト基準

1440×900：HUD 76px、左ナビ 104px、内容最大1240px。自室は絵と帳面の2列、依頼書はカード、調合は一覧と詳細。
390×844：HUD 100px以内、下ナビ68px、内容のみスクロール。自室の絵は専用枠、調合は一覧から詳細へ。
844×390：HUD 58px、左ナビ80px、本文14pxを維持して内容スクロール。固定フッターで確定。
他の検証サイズ：667×375 / 800×360 / 932×430。

色：#F6F0E4 / #25212A / #A28550 / #8F3046。本文14–16px、補足12px以上、操作44px以上。キービジュアル原本を保存し、装飾は本文の背後に重ねない。

ゲーム状態v9は変更せず、UI状態は独立したikusei-ui-v1。全行動の見込みはperformActionの非破壊実行を使う。一括調合は全数を先に検証し、0日・体力と素材は個別操作と同量。
