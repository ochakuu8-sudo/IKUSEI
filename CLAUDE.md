# デフォルメした書斎の美術（v14.6、2026-09-07）

- 依頼選択の骨格は1200×500・左に主人公と状態・右に正方形に近い3通。写実的な質感を抑え、太い輪郭、丸み、整理した陰影のゲーム用画像を採用。
- 貴族（金と藤色）、学院（銀と青緑）、商会（銅とクリーム）の便箋。系統は差出人を示し、仕事のレア度・有利不利とは無関係。文字・数値・既存の紋章を画像に焼き込まない。
- `src/art.ts` の `manorMaterialStyle` が素材参照の入口。`manor-art.css` を `manor.css` の後に読み込み、カードには紙全体、詳細には角と辺を分けた紙を使う。
- 選んだ紙から詳細へ300ms、戻る動き240ms。本文は移動中に短く切り替える。用紙内に署名と家の薔薇印を残し、520msで場面へ進む。キーボードの復帰位置、動きを減らす設定、OSの設定に対応。
- 確定時に一度だけ保存する。署名とノベルの間は受諾前の机・日付・金額を保持。結果で実際の変化を出し、結果を閉じて次の3通が見えた時点で初めてその依頼を既見として記録する。
- 紙・ペン・押印・台帳の効果音は `paperAudio.ts`。音量と消音を維持し、押印音は署名開始から280msに同期する。
- 体力不足の依頼も読める。体力不足と紹介停止は同時に表示し、受諾前に複数の代償・品位上限低下・閉じる全依頼を表示する。
- 素材一覧と正確な生成プロンプトは `ART_MANIFEST.md` 冒頭および `docs/manor-art-prompts.json`。ゲームの数値・抽選・保存v14は変更していない。

# 絵を中心にしたノベル（v14.5、2026-09-07）

**この節が現行ADVの仕様。旧版の大きいテキストボックス・中央の立ち絵を上書きする。**

- 同じ1200×500の舞台全面に背景。通常は立ち絵を右に寄せ、一枚絵では立ち絵を重ねない。
- 標準本文24px、話者16px。下端の本文領域は幅870px、原則2行。影は下150pxに限定し、標準の名前＋本文は約99px。上下に大きな帯や常設タイトルを作らない。
- `novelText.ts` で実フォント幅を測って改ページする。日本語禁則、明示改行、結合文字を保つ。長文を縮小して詰め込まない。
- タップは全文表示→次のページ。長押し・上スワイプ・Escでメニュー。常設操作は「絵だけ」「メニュー」の2つで、当たり判定は各74×74px。
- ログ・設定・絵だけ表示中は文字送りを止める。絵だけ表示を戻すタップは復帰のみ。スキップ確定は1回だけで、報酬・日付は受諾時の確定を再実行しない。
- 読書設定は22/24/26/28px、文字送り、字幕の地の濃さ、動きを減らす。従来のUI設定キーで保存し、通常・回想・新規開始で共通。
- `sceneArt.ts` に採用済みCG・構図だけ登録。CGは既定で `contain`。任意で `cover`・焦点・上字幕・立ち絵の位置を指定できる。場面中の差分は `SceneLine.visual` で指定し、次の場面ではリセットする。
- 依頼と関係イベントの境界を `SceneLine.sceneId` で保ち、通常と回想で同じ画像解決器を使う。画像を先読みし、失敗したCGは背景＋立ち絵へ戻す。発注名だけのCGを読み込まない。
- 現在CGの登録は0件。今回は新しいCGを制作せず、既存背景・立ち絵で表示を更新した。素材の採用手順は `ART_MANIFEST.md` 冒頭。
- ゲームの数値・抽選と保存v14は変更しない。`npm run test:ui` は既存45場面、5サイズ、長文、読書操作、CG切替・失敗、設定保存も検査する。

# 執務室と手紙（v14.4、2026-09-07）

**この節が現行の今日・依頼状・台帳の仕様。以下のv14.3以前の外枠・手元パネル・受諾確認ダイアログを上書きする。**

- 1200×500の同じ舞台に執務室背景 `study-v1.webp`、左の立ち絵、右の机と3枚の手紙を置く。全幅HUDや左のパネル枠は作らず、日付・残債を背景に重ねる。状態は立ち絵の裾のグラデーション上。
- カードは約265×278、傾きは1度未満。報酬・体力・代償は同じ位置で比較できる。封蝋は既存の人物紋章、紙の透かしは代償の紋。低下なしは「代償なし」。
- 手紙を開いても部屋・立ち絵・状態の位置を保つ。本文だけがスクロールし、報酬、体力、三値の前後、戻らない品位上限、紹介停止の全件、受諾操作は紙面下に常設。
- 手紙内の「この依頼を受ける」で直接確定。別の確認ダイアログは重ねない。受諾時に保存し、約300msの署名演出から既存ADV→結果へ進む。結果を閉じるまで再受諾をロック。
- 「机に戻す」かEscは同じ依頼に戻し、開いたカードへフォーカスを戻す。閲覧・台帳では日を進めない。
- 机上に「返済帳・台帳」の本と「今日は受けない」。休養も同じ紙面で体力回復と1日の消費を確認してから「今日は休む」。
- 紙の音はWeb Audioによる短い合成音。初期音量30%、設定0で消音。旧UI保存は既定値で補完、新規開始でも音量・文字速度・動き設定を維持する。
- 視覚演出は設定「動きを減らす」と `prefers-reduced-motion` に従う。音声APIが使えなくても操作できる。
- `DailyApp.tsx`・`manor.css`・`ui/paperAudio.ts` が担当。抽選・収支・三値・保存v14の形式は変更しない。
- `npm run test:ui` で1日の操作と全24依頼（17組）・5サイズ・条件の枠内表示・二重入力・演出途中の再読込・消音を検査する。

# 依頼状カード（v14.3、2026-09-07）

**この節が今日の画面の現行仕様。以下の「1依頼＝1行」と右下の手元パネルを上書きする。**

- 1200×500の固定キャンバス。左320pxに立ち絵と手元、右にほぼ正方形の依頼状3枚を横一列で置く。
- カードは種別と封蝋、依頼人と関係の輪、題名、硬貨と手取り、砂時計と消費体力、三値の紋と変化前後の値、読む導線の順。全カードで報酬と代償の位置を揃える。
- 体力の小ゲージは現在体力に対する消費割合。足りない場合は赤と「体力不足」を併記する。関係0も空の輪3つで表示する。
- 品位上限低下は「上限 −N」、紹介が閉じる場合は件数、値引きは金額横の▼で示す。理由と詳細な条件は依頼状で読める。
- 体力不足のカードも詳細を読める。受諾ボタンは無効のまま。カードを開いただけで行動を確定しない。
- 「今日は受けない」は3枚の下の独立した横長の札。1日の消費と体力回復を表示する。
- 手元には体力・所持金・三値（紋と短い名前、ゲージ、段階、数値、品位上限の刻み）と台帳への入口。全員の関係と履歴は台帳、返済と期限はHUDに置く。
- `npm run test:ui` は横3枚・ほぼ正方形・休養と手元の位置、1440×900 / 1920×1080 / 851×337 / 800×304 / 390×844での枠内表示、体力不足時の詳細閲覧と受諾禁止も確認する。
- 抽選、報酬、消費、保存形式はv14のまま。`OfferCard` / `Ledger` / `RestRow` と `chapter.css` が表示を担当する。

# 画面の情報量（v13.1、2026-09-06）

「文字が多くて頭に入らない」ため、**内部の値を記号に落とし、仕組みの説明文をUIから外した**。
以下は v13 の画面に対する上書き規則で、旧記述より優先する。

- **数える段階のあるものは記号にする。** `src/ui/symbols.tsx` の `Rings`（関係0〜3の輪）、
  `Stars` / `Pips`（v14では未使用。段階のあるものを足すときに使う）と、
  `src/marks.tsx` の紋（三軸・体力）。線画アイコンは使わない。
  **正確な数値を消すのではなく、`<details>` と台帳（約束帳・持ち物）へ移す。**
  一覧に「関係0.00」「制作経験0／次のLvまで3個」を出さない。
- **HUDの数値は4つまで**（日付・体力・所持金・残債）。返済額と残り日数は残債の下に1行で添える。
  「返済○○G／不足○○G／残債○○G」の3重表示に戻さない。
- **一覧と依頼状を同時に出さない。** 一覧と依頼状は別画面（`ui.sheet`）にする
  （企画書「1画面につき仕事は1つ」）。ナビから入ると必ず一覧に戻る。
- **一覧は1依頼＝1行。** 左に種別、中に題と相手、右に代償の紋と報酬と体力。
  買い叩きは `▼ 本日2件目` の小さな印だけ。**開かずに選べる形を崩さない。**
- **主操作は1つだけ置く。** 依頼状の「この依頼を受ける」。
  数値は札と紋に落とし、文章にしない。
- **絞り込みは置かない。** 毎日3件しか出ないので、並べ替えも検索も要らない。
- **仕組みの説明文を書かない。** 抽選の内訳や「◯◯は消費しません」の類はUIに書かない。
- **台帳は関係・跡・記録だけ。** 数値の詳細はここに置き、選ぶ画面には出さない。

# 現行実装 v14 ── 1日 = 1行動（2026-09-06）

**この節が現行仕様。v13以前の「採集→仕入れ→調合→納品」の生産ライン、スタミナ制の
3コマンド構成、章1のJSON依頼、品質・熟練・素材・在庫の記述はすべて無効。**

生産ラインは企画書に無い後付けだった。1日に2〜4回行動できる形にしないと収まらず、
そのために**1日の希少性が消え、毎日の決定が「重い1回」から「軽い3回」に薄まっていた**。
薬を作る仕組みを捨て、企画書 §3〜§6 の形に戻した。

- **毎日3件が提示される。** 母集団は現在の三軸で決まる（`Job.needs` / `Job.opensBelow`）。
  加えて常に「今日は受けない」。何も払わず、体力が戻り、1日だけを失う（§6）。
- **選ぶと日が終わる。** 1日 = 1行動。全6章 × 14日、章末に返済。
- **体力は日をまたいで持ち越し、休んだ日にだけ満タンに戻る。**
  足りない資源は日数のほうで、体力はそれを消費させる蛇口（§1-5）。
- **代償は依頼ごとに固定。** 受注時に軸を選ばせる「上乗せ」は復活させない。
  受ける前に紋と「払ったあとの値（100→88）」を必ず出す（§5）。
- **その行動で閉じる依頼を、受ける前に予告する**（`closingPreview`）。
  閉じた依頼は消さず、台帳の「もう紹介されない依頼」に跡として残す（§5）。
- **依頼を受けたら必ず場面が入る。** `sceneScript` が品の格・相手・払った軸で台本を組み、
  関係が上がった日はその人の一言が最後に付く。**毎回入ることが要件**で、初回だけにしない。
- 調剤は6種別のうちの1つ。旧設計は「素材を買って調合してから納める」前提の数字だったので、
  **素材費と調合の体力を依頼そのものに畳み込んである**（`materialCostOf` / `staminaOf`）。
  数値を作り直したのではなく、旧レシピのデータから機械的に導いている。
- 保存は `ikusei-prototype-save-v14`。v13からは返済・尊厳・評判だけを引き継ぐ
  （素材・在庫・処方・契約は対応する概念が無い）。**v13の保存は消さない。**

| ファイル | 役割 |
|---|---|
| `src/game.ts` | 依頼24件・人物・場所・台本・ノルマ。**数値はここだけ** |
| `src/daily.ts` | 1日1行動の状態遷移。提示の抽選・代償・跡・章末精算 |
| `src/saveV14.ts` | 保存の検証とv13からの移行 |
| `src/DailyApp.tsx` | 画面。今日／依頼状／台帳／精算 |
| `src/ui/scene.tsx` | ADVパート（全画面・タップ送り） |
| `src/ui/shell.tsx` | Art と Modal。`<dialog>` は自分で `--fit` をかける |
| `src/ui/symbols.tsx` | 記号（関係の輪・熟練の星・個数の丸） |
| `src/marks.tsx` | 三軸・体力・関係の紋 |

## 手元のパネルと依頼状（v14.1）

- **いま管理しているものは「手元」（`.c-ledger`）に一枚で集める。** 体力・所持金・
  章の返済と残り日数・三軸（紋／ゲージ／段階の言葉／数値・品位は上限の刻み）・
  相手ごとの関係の輪・失った依頼。散らばっていると「あと何日で何G、そのとき何が閉じているか」
  が繋がらない。**今日の画面ではHUDの体力と所持金を出さない**（同じ数字を二度書かない）。
- **依頼はボタンではなく紙にする**（`.c-slip`）。種別の札、**地に押される紋の透かし**
  （`sealOf` ＝主に削る軸、代償ゼロなら関係）、相手と関係の輪、差し出すものの紋と
  払ったあとの値、受け取る額、**残りの体力のどれだけを食うかの小さなゲージ**。
  読む前に「何を差し出す依頼か」が絵で分かることが要件。
- **「今日は受けない」も同じ紙の形で並べる**（`.c-slip.c-rest`）。選択肢の一つであって、
  画面の隅の補助操作ではない（§6）。
- 立ち絵の裾のゲージでは段階の言葉を省く（254pxにゲージと数値を通すため）。
  言葉は手元のパネルと結果画面が並べて教える。

## 場面の目録と回想（v14.2）

**ゲーム内のノベルパートは `src/scenes.ts` の目録が唯一の名簿。**
場面はこれまで `sceneScript` がその場で組むだけで識別子が無く、
「何場面あるのか」「どのCGが要るのか」を数える方法が存在しなかった。

- 目録は**依頼24 ＋ 関係21（7人×3段階）＋ 結末8（§8）＝ 53件**。
  各件が `id` / `route`（§8の筋）/ `hint`（解禁条件）/ `image`（CGの発注名）を持つ。
- **まだ書いていない場面も載せる。** 伏せると目録が制作の進捗表として使えない。
  結末8件は `lines: []` で、回想からは再生できない状態で並ぶ。
- 回想（`.c-gallery`）は**見た場面を読み返す場所であり、残りを数える場所**。
  筋で絞ると、その筋に何件あるかがそのまま出る（例：威厳は依頼3＋結末2＝5件）。
- **回想はプレイの保存とは別のキー**（`ikusei-prototype-gallery-v1`）に貯める。
  §14「引き継ぐ：回想／引き継がない：3軸・所持金・残債」。**保存を消しても回想は消さない。**
- 行動の結果は `DayOutcome.sceneIds` で「今日どの場面を見たか」を返す。
  ここを唯一の記録経路にして、画面側で条件を再実装しない。
- §14「回想の解禁は非対称にする」は `freeAfterClear` に方針だけ持たせてある。
  実際の解禁は結末の実装と同時に入れる。
- **タイトルから回想を開くときは `c-title` を外す。** `.c-title` は `display:block` ＋
  全面の覆い（`:before`）なので、付けたままだと回想が器からはみ出し、覆いが触りを全部吸う。
- **場面を閉じた指で、下の札が押されないようにする**（閉じてから350msは次を開かない）。
  最終行のタップは「閉じる」であって「次を選ぶ」ではない。

**依頼・報酬・体力・軸の数値を触ったら必ず `npm run sim`。**
企画書§4は「ノルマは代償ゼロの依頼だけを順調に重ねれば達成できる水準」。
清廉プレイの比率が100%を大きく超えるうちは、何も差し出さずに完済でき、選択が成立しない。

# CLAUDE.md

## 画面の骨格（v14でも変わらない部分）

- 画面は **1200×500 の固定キャンバス1面**。詳しくは「画面サイズ対応」。
- **立ち絵は全画面の常設レイヤー**（`.c-portrait`）。三軸は立ち絵の裾に半透明で重ねる。
  数値表示のために立ち絵と場面の絵を削らない。
- **`position: fixed` と `position: sticky` を使わない。** 変形した祖先（キャンバス）の中で
  包含ブロックと貼り付き先の解釈がブラウザごとに割れ、iOSで操作列が本文の途中に出た。
  器は素の並びで高さ100%、**操作列（`.c-footer`）は静的な最終行**、本文だけが内側でスクロールする。
- **結果はモーダルではなく全画面**（`Modal variant="result"`）。
- ナビは1層だけ（左アイコンレール）。今日の画面はレールも畳んで立ち絵を広く取る。
  台帳はHUDの日付から開く。`.route-bar` を復活させないこと。
- **ボタンは小さくしない。** 当たり判定はステージ座標で44px以上。800×304の端末では
  倍率0.61＝44pxが物理27pxしかない。ここからさらに縮めない。
  削るのはボタンではなく、余白・重複ラベル・専用の帯のほう。
- 出す情報は「毎日の判断に直結するもの」だけ。累計収入や達成率のような読み物は入れない
  （企画書が警戒している「管理ツールの型」になる）。
- ページがスクロールしない作りなので、**ブラウザのツールバーが自動で引っ込まない**。
  設定の「全画面で遊ぶ」（Fullscreen API）と `manifest.webmanifest` の `display: standalone`
  で取り返す。iOSは前者が効かないので後者が保険。`theme-color` はHUDと同じ `#302737`。
- 素材の発注仕様は `ART_MANIFEST.md`。**立ち絵は背景透過の切り抜きが前提。**
- `npm test` / `npm run sim` はNodeとTypeScriptで動作し、bashやesbuildの追加取得は不要。
  `npm run test:ui` は起動中のローカルViteへ接続する（初回のみ `npx playwright install chromium`）。

このリポジトリで作業するときの前提。**特にデプロイ周りは間違えやすいので、着手前に必ず読むこと。**

## 何のリポジトリか

`GAME_DESIGN.md` が本体（育成シミュレーションの企画書）。
`SYSTEM_PLAN.md` は、そこに足りていない「育成」の骨格をどう入れるかの方針書。
**ゲームシステムを触る前に必ず読むこと。** 段階A〜Eまで実装済み。
残りは段階F（特別依頼・突発イベント・シーン内の選択）。
`src/` は内容を触って確かめるためのブラウザ版プロトタイプ。

- Vite + React 19 + TypeScript
- 公開先: <https://ochakuu8-sudo.github.io/IKUSEI/>

## コマンド

```bash
npm ci        # 依存インストール
npm run dev   # ローカル開発サーバ
npm run build # 型チェック + 本番ビルド -> dist/
npm test      # 1日1行動エンジンの検査
npm run sim   # 章ごとの稼ぎを実測して達成率を出す（数値を触ったら必ず）
npm run test:ui # 起動中のViteに対してブラウザで1日ぶんを通す
npm run deploy # ビルドして gh-pages ブランチに公開
```

**型チェックは `tsc --noEmit -p tsconfig.app.json`。** ルートの `tsconfig.json` は
`files: []` の参照専用なので、`npx tsc --noEmit` を素で叩くと**何も検査せず成功する**。
`npm run build` を使うこと。

## デプロイの仕組み（重要）

**GitHub Pages の配信元は `gh-pages` ブランチ**（Settings → Pages が
"Deploy from a branch" 設定）。**GitHub Actions 経由ではない。**

つまり公開＝「ローカルでビルドして `gh-pages` に push」。
これを `npm run deploy` が全部やる（`scripts/deploy.sh`）。

```bash
# main に変更をマージ・push したあと
npm run deploy
```

スクリプトの動作:

1. 作業ツリーが汚れていたら中断（コミット漏れの取り違えを防ぐため）
2. `npm run build`
3. git worktree で `gh-pages` を取り出し、中身を `dist/` で丸ごと置換
4. 差分がなければ「何もせず終了」、あれば commit して push

### やってはいけないこと

- **`gh-pages` ブランチを直接編集しない。** 生成物なので次のデプロイで消える。
  変更は `src/` 側に入れて `npm run deploy` し直す。
- **`vite.config.ts` の `base: '/IKUSEI/'` を消さない。** リポジトリ名の
  サブパス配信なので、外すと本番でJS/CSSが404になる。
- **`public/.nojekyll` を消さない。** これがないと Jekyll が `assets/` を
  無視して本番だけ真っ白になる。ビルド時に `dist/` へ自動コピーされる。
- **GitHub Actions のデプロイワークフローを作り直さない。** 過去に
  `.github/workflows/deploy-pages.yml` があったが、Pages の配信元が
  ブランチ設定のため一度も動かず（手動実行が10時間以上 queued のまま
  ジョブ0件で停止）、削除済み。Actions 方式に戻すなら、先に
  Settings → Pages → Source を "GitHub Actions" に変える必要がある。
  これはリポジトリ設定の変更なので**人間の操作が必須**。

## AIエージェントの権限について

このリポジトリで作業する Claude セッションの実測値:

| 操作 | 可否 |
|---|---|
| `git push`（`main` / `gh-pages` / 作業ブランチ） | ✅ 可 |
| GitHub API でのファイル・PR・Issue 操作 | ✅ 可 |
| Actions のワークフロー/ログ**参照** | ✅ 可 |
| Actions のワークフロー**手動実行・キャンセル** | ❌ 403 |
| リポジトリ Settings の変更（Pages の配信元など） | ❌ 不可 |

デプロイが git push だけで完結する構成にしてあるのは、この権限で
最後まで完了できるようにするため。Actions に依存させると、
エージェントが自力でデプロイを完了できなくなる。

## これは何のジャンルか（設計の前提）

**成人向けの育成シミュレーション＝ビジュアルノベル寄りのゲーム。**
このジャンルの本体は**絵**であり、UIはその周りに置くもの。

- **立ち絵は日常画面の主役。** 隠したり、装飾扱いで削ったりしない。
  過去に横持ちで `display:none` にして作り直しになっている。
- **依頼を受けたら必ずイベントシーン（ADVパート）が入る。**
  全画面の1枚絵＋テキストボックス＋タップ送り。ここが「本番」で、
  遊ぶ場所ではなく観る場所。選択肢は出さない。
- 数値や表は、絵の邪魔をしない位置に半透明で重ねる。

## 画面構成（重要）

**商業スマホゲームの作法に合わせ、画面を遷移させる。**
複数のパネルを並べて同時にスクロールさせる作りにはしない
（あれは管理ツールの型で、一度それを作って作り直している）。

```
タイトル
  └ 今日 (today) ─ 立ち絵＋HUD＋「今日の3件」＋「今日は受けない」
       └ 依頼状 (sheet) ─ 受け取る額・使う体力・差し出すものを断言して見せる
            └ 場面 (scene) ─ 全画面の絵＋テキスト送り。選択肢は出さない
                 └ 結果 (result) ─ 報酬・軸の低下・品位上限・閉じた依頼
  └ 台帳 (journal) ─ 関係／もう紹介されない依頼／記録
  └ 章末 (settlement) ─ 返済の確定
```

**薬の注文と身体の依頼を別画面に分けない。** 同じ卓の上で比べるのがこのゲームの中身。

### 仕事の選択を、場所の巡回にしない

**毎日の依頼選びは「今日」の一画面で完結させる。**
場所を順に開いて回る形にすると、意思決定の無い手間だけが増える
（一度そう作って作り直している）。

提示は3件だけ。**3件とも受けたくない日が来ることが見せ場**なので、
件数を増やして逃げ道を作らない。抽選は `runId:章:日` から決めるので、
読み込み直しても同じ顔ぶれが出る。

### 章とノルマ

**全6章 × 14日。章末に `QUOTAS` の額を納める。**
企画書§4は「章末の返済額は、代償ゼロの依頼だけを順調に重ねれば達成できる水準」。
`npm run sim` の清廉プレイの比率がその指標で、いまは 231%→104%（第1章→第6章）。
前半は余裕があり、後半で初めて代償が魅力的になる形にはなっているが、
**まだ清廉のまま完済できる。** ここを詰めるのが次のバランス作業。

未達なら**利息25%が付いて残債が増え**、次章のノルマに丸ごと乗る。
さらに威厳−15・品位−10 を受け、品位が下がるぶん軽い依頼から閉じていく。

**依頼・報酬・体力・軸を触ったら、必ず `npm run sim` で測り直すこと。**
比率が崩れると章構成の意味が消える。

## 稼ぎ方は6種別（`src/game.ts` の `jobs`）

依頼は **調剤／親交／社交／実務／人前／労務／裏** の種別を持ち、**同じ一画面に並ぶ。**
薬の注文も身体の依頼も、同じ卓の上で比べるのがこのゲームの中身。

```
代償ゼロの仕事   体力を多く使う。報酬は低い（実務・親交・調剤の一部）
軸を払う仕事     体力は同じか少なく、報酬が高い（社交・人前・裏）
```

**薬を「作る」仕組みは廃止した。** 採集・仕入れ・調合・在庫・品質・熟練は無い。
調剤は「その薬を納める依頼」であって、素材費と調合の体力は依頼に畳み込んである。
生産ラインを戻すと1日1行動が壊れるので、復活させないこと。

### 代償は依頼ごとに固定。プレイヤーには選ばせない

**上乗せ（受注時にどの軸を払うか選ぶ仕組み）は廃止した。**
シミュレーションで壊れていた原因がほぼ全部そこにあった ──
払う方が常に有利、1軸を捨てるのが最適、技能不足を肩代わりするので
育成が無意味になっていた。

いまは `Job.costs` に**その依頼が必ず削る軸と量**が入っている（空なら無償）。
選択は「どの依頼を受けるか」に集約する。

**技能（恒久成長）は廃止した。** 資源をやりくりするゲームに、育て切ったら
終わりの成長軸を混ぜると、後半が「同じ最適解の繰り返し」になる。
**依頼の開閉は尊厳そのものが決める。**

### 仕事一覧には「何を失うか」を必ず出す

比べる画面に**削られる軸と量、払ったあとの値（`62→51`）**を並べる。
報酬とスタミナだけの一覧にすると、1件ずつ開かないと価格が分からず、
比較画面として成立しない（一度そう作って作り直している）。

カードは `grid-template-columns: repeat(auto-fill, minmax(...))` で**横幅を使い切る**。
横持ちで1列にすると画面の半分が空く。ただし**1枚を横長の帯にしない** ──
1枚が依頼状に見える幅まで詰めて枚数を出す（列の下限は
通常 224px／`max-height:560px` で 196px／縦持ちで 164px）。

`App.tsx` の `View` 型がこの遷移そのもの。**1画面につき仕事は1つ。**

### 軸は名前ではなく紋（マーク）で出す ── `src/marks.tsx`

三軸と体力・関係には**紋**がある。`<Mark name="貞操" />` で描く。

| 名前 | 紋 | 意味 |
|---|---|---|
| 貞操 | 百合 | 純潔。花弁が閉じたまま帯で縛られている |
| 品位 | 小冠 | 人前に出られる身分 |
| 威厳 | 盾に横帯 | 名前で立てる格 |
| 体力 | 砂時計 | 一日は一度しか使えない |
| 関係 | 二つの輪 | 人と結んだぶんだけ重なる |

- **線画アイコン（lucide 等）を軸に使わない。** アイコンを並べると画面が
  アプリの一覧＝「情報」に見える。ここは紙なので、紙に押される紋章＝
  **塗りのシルエット**にしてある。`Zap` を体力に使っていたのを置き換えた。
- 色を持たない。`fill: currentColor` なので、置いた場所の文字色（軸色）を継ぐ。
- **依頼カードでは軸名を書かず紋だけ**にする（`貞操 −16` ではなく `［百合］−16`）。
  一覧の文字量が半分になり、比較画面が読み物ではなく帳簿として成立する。
  紋と名前の対応は**自室のゲージ・契約シート・結果画面**が並べて教えている
  ので、この3画面から紋を外さないこと。
- 依頼カードの地には、その依頼が主に削る軸の紋が**透かし**で入る（`.jc-seal`）。
  代償ゼロの依頼は関係の紋。読む前に「何を差し出す依頼か」が絵で分かる。

### 尊厳の状態で、受けられる「種類」が入れ替わる

**これがこのゲームの背骨。** 依頼は2つの条件を持つ（`isOpen`）。

- `needs` … これを下回ると**紹介されなくなる**（軸ごとの下限）
- `opensBelow` … これ以下まで落ちて、**初めて回ってくる**（裏の仕事）

依頼には `kind`（親交／社交／実務／人前／労務／裏）があり、
仕事一覧の上に**種別ごとの件数**が出る。減るだけでなく、顔ぶれが変わる。

| 状態 | 内訳 | 最高額 |
|---|---|---|
| 無傷 100/100/100 | 親交2 社交3 実務2 人前3 労務1 | 340G |
| 貞操 44 | ↑から実務1減／**裏1が出現** | 340G |
| 貞操 0 | 人前3 労務1 裏1（**親交・社交・実務が全滅**） | 300G |
| 全部 0 | **労務1 裏2** | 300G |

**払った軸が、自分で自分の稼ぎ口を閉じる。** 貞操を売るとまともな仕事が消え、
威厳を売ると名前で稼ぐ仕事が消え、品位を売ると人前の仕事が消える。
かわりに**裏の仕事が開く** ── 実入りはいいが、戻り道は無い。

**親交の席は、まともに扱われているうちしか呼ばれない。** 代償ゼロ・低報酬だが
関係が2進む（`bond`）。落ちると関係を育てる手段そのものを失う。

**閾値を外すと、軸を捨てるのがタダになってゲームが崩れる。**

### 依頼は常設。毎日の抽選ではない

依頼は**人物**が抱えていて（`jobsBy`）、**開いているものはいつでも受けられる**。
「堕ちて開く」方向（低い軸で新しい仕事が現れる）は未実装 ── `SYSTEM_PLAN.md` の段階4。

**同じ人に通い詰めると報酬が下がる**（`fatigueRate`。直近3日ぶんと本日ぶんの**納品回数**で
−18%/−32%/−42%）。日単位で数えると、同日反復を許した現行仕様では一度も効かない。
常設リストが「一番おいしい依頼の繰り返し」に潰れるのを防ぐための仕掛けなので、
外すならかわりの歯止めが要る。

### 関係は組織ではなく人と結ぶ

`people`（6人）が場所に属し、**依頼は人物に紐づく**（`Job.person`）。
関係は0〜3で、`relationStage()` が言葉にする（依頼人／顔を覚えられた／
名前で呼ばれる／私的な用も頼まれる）。

**段階が上がった日は、その人の一言がシーンの最後に入る**
（`stageUpLine`。台詞は `people[].stageLines`）。
数値が増えるだけだと手応えが無いので、**増えたら必ず絵か台詞で返す**。

### 崩してはいけない不変条件

- **どの画面もページスクロールを発生させない。** `.screen` が `100dvh`。
  はみ出したらレイアウトの負けなので、要素を減らすか畳む。
- **依頼ごとの代償は、受ける前に必ず断言して見せること。** 何を差し出すかが
  このゲームの中身なので、一覧にも確認シートにも出す。隠して受けさせない。
- 主要動作（受注ボタン）が画面外に出てはいけない。
- 立ち絵とイベントCGを、数値表示のために削らない。

## 絵の差し替え（`src/art.ts`）

**個別の背景・立ち絵差分・シーン素材は未実装。** 共通画像には採用予定の主人公キービジュアル `public/hero-key-visual.webp` を使用する。
`<img onError>` で候補を順に試すので、**描けたぶんから1枚ずつ置くだけで反映される。**
コード側の変更は不要。

| 置き場所 | 用途 | 推奨サイズ |
|---|---|---|
| `public/art/bg/<id>.png` | 背景（`mansion` / `title` / `ending`） | 1920×1080 |
| `public/art/map.png` | 街の地図（マーカーは割合配置なので合う） | 1920×1080 |
| `public/art/place/<placeId>.png` | 場所の背景 | 1920×1080 |
| `public/art/portrait/<stage>.png` | 立ち絵 | 1024×1536・背景透過 |
| `public/art/person/<personId>.png` | 依頼人の顔（無ければ頭文字の丸） | 512×512 |
| `public/art/scene/<jobId>.png` | イベントCG（依頼共通） | 1920×1080 |
| `public/art/scene/<jobId>-<axis>.png` | 軸ごとの差分（あれば優先） | 1920×1080 |

- `<stage>` は `intact` / `worn` / `fallen` / `ruined`。**3軸のうち最も低いもの**で決まる。
- `<axis>` は `貞操` / `品位` / `威厳`。
- **背景と依頼人の顔は仮画像に落とさない**（背景は立ち絵と二重に写り、
  顔は6人全員が同じ絵になるため）。背景は `.backdrop` のグラデーション、
  顔は頭文字の丸が出る。
- `Art` は候補URLを**重複除去してから**順に試す。同じURLが並ぶと `src` が
  変わらず `onError` が二度と起きないので、候補送りがそこで止まる（実際に
  軸差分の無い依頼でシーンCGが出なくなっていた）。

## コードの構成

| ファイル | 役割 |
|---|---|
| `src/game.ts` | 型・場所・人物・依頼24件・ノルマ・台本。**数値と文言はここだけ** |
| `src/daily.ts` | 1日1行動の状態遷移。提示の抽選・代償・跡・章末精算 |
| `src/saveV14.ts` | 保存の検証とv13からの移行 |
| `src/DailyApp.tsx` | 画面。今日／依頼状／台帳／精算／結果 |
| `src/ui/scene.tsx` | ADVパート。`src/ui/shell.tsx` は Art と Modal |
| `src/marks.tsx` | 三軸・体力・関係の紋。`src/ui/symbols.tsx` は輪・星・丸 |
| `src/chapter.css` | 画面のスタイル。色と字送りは `src/styles.css` の `:root` |
| `scripts/sim.mjs` | 章ごとの稼ぎの実測。`npm run sim`。**数値を触ったら必ず走らせる** |
| `SYSTEM_PLAN.md` | 育成システムの改善方針。**ゲームシステムを触る前に読む** |

## 画面サイズ対応

**画面全体を 1200×500（2.4:1）の固定キャンバスに固定している。**
端末ごとにレイアウトを組み替えるのをやめ、`--fit = min(innerWidth/1200, innerHeight/500)`
で丸ごと拡大縮小する。余った領域は上下または左右の余白（レターボックス）になる。

- **端末別のメディアクエリは廃止した。** 旧 `max-height:500px` の詰めルールは
  常に成立するので素の規則へ展開し、`max-width` 系の分岐は削除した。
  CSSに端末幅の分岐を足さないこと。設計の基準は 1200×500 の1面だけ。
- **キャンバスは自前で中央に置く**（`position:absolute; left/top:50%;
  transform: translate(-50%,-50%) … scale(var(--fit))`）。
  グリッドや `margin:auto` の中央寄せは、要素が画面より大きいと開始辺に
  寄せられて右側が切れる（実際に 851×337 でそうなった）。
- **`<dialog>` は `showModal()` で top layer に出るので、親の transform が効かない。**
  確認ダイアログ・シーン・結果には同じ `--fit` と中央寄せを自分にかけている。
  ここを外すとシーンだけレターボックスの外に出る。
- **セーフエリアはキャンバスの外側で避ける。** `.app` に
  `env(safe-area-inset-*)` の padding を足すと 1200×500 の設計が歪むので、
  App.tsx がプローブ要素から実測して `--fit` と `--shift-x/y` に反映する。
- **縦持ちでも同じキャンバスをそのまま出す。** 回転を強いる案内は置かない ──
  横に持ち替えるかどうかはプレイヤーが決める。縦持ちでは幅に合わせて縮み、
  上下が大きな余白になる（390×760で 390×163）。
- 器の高さは `.stage` が確定させる。`.content-inner` の内側スクロールが
  そこにぶら下がっているので、`.stage` の `height:100%` を外すと
  章末の「確定」など主要動作が折り返しの下に落ちる。

変更したら `1440×900 / 1920×1080 / 851×337 / 800×304` で、
**全画面（タイトル・今日・依頼状・台帳・精算・場面・結果）**の
はみ出しが無いことを確認する。ページスクロールが0なだけでは不十分で、
**主要動作（この依頼を受ける・返済の確定）が折り返しの上にあること**まで見る。

## 書き方の決まり

- UI文言・企画書は日本語。コード内の識別子とコミットメッセージは英語。
- **数値バランスは `GAME_DESIGN.md` が正。** プロトタイプ側だけ勝手に変えない。
  変更したら企画書の該当節も直す（コード内に `§` 番号でコメントを残してある）。
- セーブは `localStorage` の `ikusei-prototype-save-v14`。`DailyState` の形を
  変えたら**キーの版番号を上げる**。古いセーブが読み込まれて壊れる。
- マイナスは全角（`−`）で統一。ASCIIハイフンと混ぜない。

## 企画書との対応（実装済みの要件）

`GAME_DESIGN.md` §10「UI」の要求は一通り入っている。壊さないこと。

- 残債を画面で最も大きい数値にする（§9「常に、最も大きく出す」）
- 3軸は数値だけでなく**段階を言葉で**出す（`axisStage`）
- **品位は現在値と上限を両方**出す。上限はゲージ右端に斜線で残す（§1-4「上限は戻らない」）
- 依頼ごとの代償を**一覧と確認シートの両方に**出す。払うものを隠さない（§5）
- スタミナが足りない依頼は**選べない**（§1-5）
- 尊厳が下がると上の依頼から閉じ、最後は荷造りだけが残る（§5）
- 紹介されなくなった依頼は消さず**跡として残す**（§5）
- 結果画面に報酬内訳と軸の低下を並べ、**品位の上限低下は別枠**にする（§10）
- 依頼を受けたら**イベントシーンを挟む**。「本番は観るもの」（§5）

## UI設計の追補

UI_REDESIGN.md は v12 の再設計メモで、いまの画面とは対応しない。
現行の指針は本書の「画面の情報量」と「画面の骨格」を正とする。
`uiState.ts` / `presentation.ts` / `workflow.ts` / `engine.ts` は v14 で削除した。
表示設定は `ikusei-prototype-ui-v14` に別保存する。
