# CLAUDE.md

## 現行システムの追補（2026-09-05）

この追補は下記の古い構成・セーブ・収入の説明に優先する。

- 人物・素材・処方は仮データ。支援の割当は src/content/support.ts、型は supportTypes.ts。
- 状態更新は engine.ts の performAction、約束の判定は contracts.ts。App.tsxで資源更新を複製しない。
- 現行仕様と仮数値は GAME_DESIGN.md §15、SYSTEM_PLAN.md §10。
- 通常依頼は事前受注なし。特別依頼は受付期間・前金・指定日当日限定。薬以外の頼まれごとも「仕事をする」の統合一覧へ。人物画面は相手別フィルターの入口。
- まとめ納品はdelivery.tsで全件を事前検証し、engine.tsで資源・履行・報酬を一括適用、日末処理を1回だけ行う。
- 特別依頼の報酬と0日イベントはrewards.ts/content/events.ts。未再生本文も保存し、再読込で消さない。
- 保存は ikusei-prototype-save-v9。save.tsでv7/v8を移行し、受諾済み旧契約の条件を保持する。新しい解禁は推測しない。
- npm test / npm run sim はNodeとTypeScriptで動作し、bashやesbuildの追加取得は不要。
- npm run test:ui は起動中のローカルViteへ接続する。初回のみ npx playwright install chromium。
- 結果と章末の長い説明は1か所でスクロールし、進むボタンは常に表示する。ページ全体はスクロールさせない。
- 既存ノルマは暫定維持。旧simの「清廉なら248G余り」は現在の難易度の結論として扱わない。
- 竜涎の関係供給は未実装。今回の一般的な素材支援機構と、個別素材の供給データを混同しない。


このリポジトリで作業するときの前提。**特にデプロイ周りは間違えやすいので、着手前に必ず読むこと。**

## 何のリポジトリか

`GAME_DESIGN.md` が本体（育成シミュレーションの企画書）。
`SYSTEM_PLAN.md` は、そこに足りていない「育成」の骨格をどう入れるかの方針書。
**ゲームシステムを触る前に必ず読むこと。** 段階A〜Eまで実装済み。
残りは段階F（特別依頼・突発イベント・シーン内の選択）。
`src/` は内容を触って確かめるためのブラウザ版プロトタイプ。

- Vite + React 19 + TypeScript
- 公開先: <https://ochakuu8-sudo.github.io/IKUSEI/>

## コマンド

```bash
npm ci        # 依存インストール
npm run dev   # ローカル開発サーバ
npm run build # 型チェック + 本番ビルド -> dist/
npm run sim   # 章ごとの稼ぎを実測して達成率を出す（数値を触ったら必ず）
npm run deploy # ビルドして gh-pages ブランチに公開
```

**型チェックは `tsc --noEmit -p tsconfig.app.json`。** ルートの `tsconfig.json` は
`files: []` の参照専用なので、`npx tsc --noEmit` を素で叩くと**何も検査せず成功する**。
`npm run build` を使うこと。

## デプロイの仕組み（重要）

**GitHub Pages の配信元は `gh-pages` ブランチ**（Settings → Pages が
"Deploy from a branch" 設定）。**GitHub Actions 経由ではない。**

つまり公開＝「ローカルでビルドして `gh-pages` に push」。
これを `npm run deploy` が全部やる（`scripts/deploy.sh`）。

```bash
# main に変更をマージ・push したあと
npm run deploy
```

スクリプトの動作:

1. 作業ツリーが汚れていたら中断（コミット漏れの取り違えを防ぐため）
2. `npm run build`
3. git worktree で `gh-pages` を取り出し、中身を `dist/` で丸ごと置換
4. 差分がなければ「何もせず終了」、あれば commit して push

### やってはいけないこと

- **`gh-pages` ブランチを直接編集しない。** 生成物なので次のデプロイで消える。
  変更は `src/` 側に入れて `npm run deploy` し直す。
- **`vite.config.ts` の `base: '/IKUSEI/'` を消さない。** リポジトリ名の
  サブパス配信なので、外すと本番でJS/CSSが404になる。
- **`public/.nojekyll` を消さない。** これがないと Jekyll が `assets/` を
  無視して本番だけ真っ白になる。ビルド時に `dist/` へ自動コピーされる。
- **GitHub Actions のデプロイワークフローを作り直さない。** 過去に
  `.github/workflows/deploy-pages.yml` があったが、Pages の配信元が
  ブランチ設定のため一度も動かず（手動実行が10時間以上 queued のまま
  ジョブ0件で停止）、削除済み。Actions 方式に戻すなら、先に
  Settings → Pages → Source を "GitHub Actions" に変える必要がある。
  これはリポジトリ設定の変更なので**人間の操作が必須**。

## AIエージェントの権限について

このリポジトリで作業する Claude セッションの実測値:

| 操作 | 可否 |
|---|---|
| `git push`（`main` / `gh-pages` / 作業ブランチ） | ✅ 可 |
| GitHub API でのファイル・PR・Issue 操作 | ✅ 可 |
| Actions のワークフロー/ログ**参照** | ✅ 可 |
| Actions のワークフロー**手動実行・キャンセル** | ❌ 403 |
| リポジトリ Settings の変更（Pages の配信元など） | ❌ 不可 |

デプロイが git push だけで完結する構成にしてあるのは、この権限で
最後まで完了できるようにするため。Actions に依存させると、
エージェントが自力でデプロイを完了できなくなる。

## これは何のジャンルか（設計の前提）

**成人向けの育成シミュレーション＝ビジュアルノベル寄りのゲーム。**
このジャンルの本体は**絵**であり、UIはその周りに置くもの。

- **立ち絵は日常画面の主役。** 隠したり、装飾扱いで削ったりしない。
  過去に横持ちで `display:none` にして作り直しになっている。
- **依頼を受けたら必ずイベントシーン（ADVパート）が入る。**
  全画面の1枚絵＋テキストボックス＋タップ送り。ここが「本番」で、
  遊ぶ場所ではなく観る場所。選択肢は出さない。
- 数値や表は、絵の邪魔をしない位置に半透明で重ねる。

## 画面構成（重要）

**商業スマホゲームの作法に合わせ、画面を遷移させる。**
複数のパネルを並べて同時にスクロールさせる作りにはしない
（あれは管理ツールの型で、一度それを作って作り直している）。

```
タイトル
  └ 自室 (home) ─ 立ち絵＋HUD＋「仕事を受ける／調合する／出かける／休む」
       ├ 仕事を受ける (jobs) ─ 薬の注文も身体の依頼も同じ一画面に並べる
       │    └ 契約シート (contract) ─ 代償と、納める品を断言して見せる
       │         └ イベントシーン (scene) ─ 全画面CG＋テキスト送り
       │              └ 結果 (result) ─ 報酬内訳・軸の低下・覚えた処方
       ├ 調合する (brew) ─ レシピ帳と手持ちの素材。日は進まない
       └ 地図 (map) ─ 場所のマーカーを選ぶ
            ├ 場所 (place) ─ そこにいる人。関係を進める／商会では素材を仕入れる
            └ 採集地 (place, kind:'gather') ─ 体力を払って素材を採る
```

**薬の注文と身体の依頼を別画面に分けない。** 同じ卓の上で比べるのがこのゲームの中身。

### 仕事の選択を、地図の巡回にしない

**毎日の依頼選びは「仕事を受ける」一画面で完結させる。**
場所を順に開いて回る形にすると、意思決定の無い手間だけが増える
（一度そう作って作り直している）。

地図は**出向く理由があるときに行く場所**。人に会い、関係を進め、
将来は特別依頼とイベントがここに乗る。

### 章とノルマ

**全6章 × 14日。章末に `QUOTAS` の額を納める**（`settlementOf` / `applySettlement`）。
ノルマは実測（正攻法は1章あたり約1,800G）から逆算してあり、
**正攻法の収入に対する比率が 166% → 75% へ落ちる**。前半は余裕があり
育成に日を回せるが、後半は正攻法だけでは届かない。

未達なら**利息25%が付いて残債が増え**、次章のノルマに丸ごと乗る。
さらに威厳−15・品位−10 を受け、品位が下がるぶん軽い依頼から閉じていく。

**依頼・処方・素材・報酬を触ったら、必ず `npm run sim` で測り直してノルマを
引き直すこと。** 比率が崩れると章構成の意味が消える。
実測値と引き直しの手順は `SYSTEM_PLAN.md` §4。

## 主軸は調剤（`SYSTEM_PLAN.md` §2）

**薬を作って納めるのが正攻法の稼ぎ口。** 身体を売る依頼は、間に合わないときの道。

```
素材を手に入れる（1日）  買う＝金＋商会へ行く ／ 採る＝体力＋採集地へ行く
調合する（日は進まない） 体力と素材を使う。何度でも打てる
納める（1日）            依頼書として来る。受けるのは他の依頼と同じ画面
```

- **レシピは金では買えない。** 人間関係の段階（`Person.teaches`）と依頼の報酬
  （`Job.teaches`）でしか手に入らない。`RECIPE_SOURCE` に「どこで覚えたか」が入っていて、
  レシピ帳が**彼女がどこを通ってきたかの記録**になる。ここを金で買えるようにしない
- **一つの素材が二つの品に分岐する。** 乾燥ケシは眠り薬にも媚薬にもなる。
  手持ちは一つなので両方は作れない。**この競合を外すとお使いゲームになる**
- **品には格（`Grade`）がある。** 下の格（媚薬・堕胎薬）だけが `costs` で威厳を削り、
  威厳が落ちると `needs` を持つ上の注文（香油・気付け薬）が閉じる。
  **作った物のいちばん下が、上の客を追い払う**
- 竜涎（媚薬の材料）は**裏通り（威厳30以下で開く）か、マルクとの関係**でしか出ない。
  堕ちて開く道と育てて開く道が、同じ素材に繋がっている

### 代償は依頼ごとに固定。プレイヤーには選ばせない

**上乗せ（受注時にどの軸を払うか選ぶ仕組み）は廃止した。**
シミュレーションで壊れていた原因がほぼ全部そこにあった ──
払う方が常に有利、1軸を捨てるのが最適、技能不足を肩代わりするので
育成が無意味になっていた。

いまは `Job.costs` に**その依頼が必ず削る軸と量**が入っている（空なら無償）。
選択は「どの依頼を受けるか」に集約する。

**技能（恒久成長）は廃止した。** 資源をやりくりするゲームに、育て切ったら
終わりの成長軸を混ぜると、後半が「同じ最適解の繰り返し」になる。
**依頼の開閉は尊厳そのものが決める。**

### 仕事一覧には「何を失うか」を必ず出す

比べる画面に**削られる軸と量、払ったあとの値（`62→51`）**を並べる。
報酬とスタミナだけの一覧にすると、1件ずつ開かないと価格が分からず、
比較画面として成立しない（一度そう作って作り直している）。

カードは `grid-template-columns: repeat(auto-fill, minmax(...))` で**横幅を使い切る**。
横持ちで1列にすると画面の半分が空く。ただし**1枚を横長の帯にしない** ──
1枚が依頼状に見える幅まで詰めて枚数を出す（列の下限は
通常 224px／`max-height:560px` で 196px／縦持ちで 164px）。

`App.tsx` の `View` 型がこの遷移そのもの。**1画面につき仕事は1つ。**

### 軸は名前ではなく紋（マーク）で出す ── `src/marks.tsx`

三軸と体力・関係には**紋**がある。`<Mark name="貞操" />` で描く。

| 名前 | 紋 | 意味 |
|---|---|---|
| 貞操 | 百合 | 純潔。花弁が閉じたまま帯で縛られている |
| 品位 | 小冠 | 人前に出られる身分 |
| 威厳 | 盾に横帯 | 名前で立てる格 |
| 体力 | 砂時計 | 一日は一度しか使えない |
| 関係 | 二つの輪 | 人と結んだぶんだけ重なる |

- **線画アイコン（lucide 等）を軸に使わない。** アイコンを並べると画面が
  アプリの一覧＝「情報」に見える。ここは紙なので、紙に押される紋章＝
  **塗りのシルエット**にしてある。`Zap` を体力に使っていたのを置き換えた。
- 色を持たない。`fill: currentColor` なので、置いた場所の文字色（軸色）を継ぐ。
- **依頼カードでは軸名を書かず紋だけ**にする（`貞操 −16` ではなく `［百合］−16`）。
  一覧の文字量が半分になり、比較画面が読み物ではなく帳簿として成立する。
  紋と名前の対応は**自室のゲージ・契約シート・結果画面**が並べて教えている
  ので、この3画面から紋を外さないこと。
- 依頼カードの地には、その依頼が主に削る軸の紋が**透かし**で入る（`.jc-seal`）。
  代償ゼロの依頼は関係の紋。読む前に「何を差し出す依頼か」が絵で分かる。

### 尊厳の状態で、受けられる「種類」が入れ替わる

**これがこのゲームの背骨。** 依頼は2つの条件を持つ（`isOpen`）。

- `needs` … これを下回ると**紹介されなくなる**（軸ごとの下限）
- `opensBelow` … これ以下まで落ちて、**初めて回ってくる**（裏の仕事）

依頼には `kind`（親交／社交／実務／人前／労務／裏）があり、
仕事一覧の上に**種別ごとの件数**が出る。減るだけでなく、顔ぶれが変わる。

| 状態 | 内訳 | 最高額 |
|---|---|---|
| 無傷 100/100/100 | 親交2 社交3 実務2 人前3 労務1 | 340G |
| 貞操 44 | ↑から実務1減／**裏1が出現** | 340G |
| 貞操 0 | 人前3 労務1 裏1（**親交・社交・実務が全滅**） | 300G |
| 全部 0 | **労務1 裏2** | 300G |

**払った軸が、自分で自分の稼ぎ口を閉じる。** 貞操を売るとまともな仕事が消え、
威厳を売ると名前で稼ぐ仕事が消え、品位を売ると人前の仕事が消える。
かわりに**裏の仕事が開く** ── 実入りはいいが、戻り道は無い。

**親交の席は、まともに扱われているうちしか呼ばれない。** 代償ゼロ・低報酬だが
関係が2進む（`bond`）。落ちると関係を育てる手段そのものを失う。

**閾値を外すと、軸を捨てるのがタダになってゲームが崩れる。**

### 依頼は常設。毎日の抽選ではない

依頼は**人物**が抱えていて（`jobsBy`）、**開いているものはいつでも受けられる**。
「堕ちて開く」方向（低い軸で新しい仕事が現れる）は未実装 ── `SYSTEM_PLAN.md` の段階4。

**同じ人に通い詰めると報酬が下がる**（`fatigueRate`。直近3日で −18%/−32%/−42%）。
常設リストが「一番おいしい依頼の繰り返し」に潰れるのを防ぐための仕掛けなので、
外すならかわりの歯止めが要る。

### 関係は組織ではなく人と結ぶ

`people`（6人）が場所に属し、**依頼は人物に紐づく**（`Job.person`）。
関係は0〜3で、`relationStage()` が言葉にする（依頼人／顔を覚えられた／
名前で呼ばれる／私的な用も頼まれる）。

**段階が上がった日は、その人の一言がシーンの最後に入る**
（`stageUpLine`。台詞は `people[].stageLines`）。
数値が増えるだけだと手応えが無いので、**増えたら必ず絵か台詞で返す**。

### 崩してはいけない不変条件

- **どの画面もページスクロールを発生させない。** `.screen` が `100dvh`。
  はみ出したらレイアウトの負けなので、要素を減らすか畳む。
- **依頼ごとの代償は、受ける前に必ず断言して見せること。** 何を差し出すかが
  このゲームの中身なので、一覧にも確認シートにも出す。隠して受けさせない。
- 主要動作（受注ボタン）が画面外に出てはいけない。
- 立ち絵とイベントCGを、数値表示のために削らない。

## 絵の差し替え（`src/art.ts`）

**個別の背景・立ち絵差分・シーン素材は未実装。** 共通画像には採用予定の主人公キービジュアル `public/hero-key-visual.webp` を使用する。
`<img onError>` で候補を順に試すので、**描けたぶんから1枚ずつ置くだけで反映される。**
コード側の変更は不要。

| 置き場所 | 用途 | 推奨サイズ |
|---|---|---|
| `public/art/bg/<id>.png` | 背景（`mansion` / `title` / `ending`） | 1920×1080 |
| `public/art/map.png` | 街の地図（マーカーは割合配置なので合う） | 1920×1080 |
| `public/art/place/<placeId>.png` | 場所の背景 | 1920×1080 |
| `public/art/portrait/<stage>.png` | 立ち絵 | 1024×1536・背景透過 |
| `public/art/person/<personId>.png` | 依頼人の顔（無ければ頭文字の丸） | 512×512 |
| `public/art/scene/<jobId>.png` | イベントCG（依頼共通） | 1920×1080 |
| `public/art/scene/<jobId>-<axis>.png` | 軸ごとの差分（あれば優先） | 1920×1080 |

- `<stage>` は `intact` / `worn` / `fallen` / `ruined`。**3軸のうち最も低いもの**で決まる。
- `<axis>` は `貞操` / `品位` / `威厳`。
- **背景と依頼人の顔は仮画像に落とさない**（背景は立ち絵と二重に写り、
  顔は6人全員が同じ絵になるため）。背景は `.backdrop` のグラデーション、
  顔は頭文字の丸が出る。
- `Art` は候補URLを**重複除去してから**順に試す。同じURLが並ぶと `src` が
  変わらず `onError` が二度と起きないので、候補送りがそこで止まる（実際に
  軸差分の無い依頼でシーンCGが出なくなっていた）。

## コードの構成

| ファイル | 役割 |
|---|---|
| `src/game.ts` | 型・場所・人物・依頼データ・ルール・台本。**数値と判定はここだけ** |
| `src/art.ts` | 絵のパス規約とフォールバック。**画像の参照は必ずここ経由** |
| `src/marks.tsx` | 三軸・体力・関係の紋（インラインSVG）。**軸にアイコンを使うならここ** |
| `scripts/sim.mjs` | 章ごとの稼ぎの実測。`npm run sim`。**数値を触ったら必ず走らせる** |
| `SYSTEM_PLAN.md` | 育成システムの改善方針。**ゲームシステムを触る前に読む** |
| `src/App.tsx` | 画面。`View` の状態機械と各画面のコンポーネント |
| `src/styles.css` | 全スタイル。色と字送りは `:root` のCSS変数 |

## 画面サイズ対応

**想定する最小環境は「スマホ横持ち 667×375」。**

**分岐は幅ではなく高さを先に見る。** 横持ちで足りないのは幅(667〜932px)
ではなく高さ(360〜430px)なので、`@media (max-height: 560px)` が主分岐。
幅だけで分岐すると横持ちが「狭い画面」と誤判定される（実際に一度そうなった）。

| 条件 | レイアウト |
|---|---|
| `max-height: 560px` | 文字と余白を縮める。`:root` のトークンを上書きする |
| ↑かつ `max-width: 720px` | 立ち絵を細く、コマンド列を広く。上乗せの台詞は落とす |
| `max-aspect-ratio: 3/4` | 縦持ち。立ち絵を上、コマンドを下に積む |

変更したら `667×375 / 800×360 / 844×390 / 932×430 / 390×844 / 1440×900`
で、**全画面（タイトル・日常・契約・シーン・結果・エンディング）**の
はみ出しが無いことを必ず確認する。

## 書き方の決まり

- UI文言・企画書は日本語。コード内の識別子とコミットメッセージは英語。
- **数値バランスは `GAME_DESIGN.md` が正。** プロトタイプ側だけ勝手に変えない。
  変更したら企画書の該当節も直す（コード内に `§` 番号でコメントを残してある）。
- セーブは `localStorage` の `ikusei-prototype-save-v7`。`GameState` の形を
  変えたら**キーの版番号を上げる**。古いセーブが読み込まれて壊れる。
- マイナスは全角（`−`）で統一。ASCIIハイフンと混ぜない。

## 企画書との対応（実装済みの要件）

`GAME_DESIGN.md` §10「UI」の要求は一通り入っている。壊さないこと。

- 残債を画面で最も大きい数値にする（§9「常に、最も大きく出す」）
- 3軸は数値だけでなく**段階を言葉で**出す（`axisStage`）
- **品位は現在値と上限を両方**出す。上限はゲージ右端に斜線で残す（§1-4「上限は戻らない」）
- 依頼ごとの代償を**一覧と確認シートの両方に**出す。払うものを隠さない（§5）
- スタミナが足りない依頼は**選べない**（§1-5）
- 尊厳が下がると上の依頼から閉じ、最後は荷造りだけが残る（§5）
- 紹介されなくなった依頼は消さず**跡として残す**（§5）
- 結果画面に報酬内訳と軸の低下を並べ、**品位の上限低下は別枠**にする（§10）
- 依頼を受けたら**イベントシーンを挟む**。「本番は観るもの」（§5）

## UI設計の追補

UI全面再設計についてはUI_REDESIGN.mdを参照。Appの画面分岐をsrc/uiへ分離し、旧styles.cssを置換。過去の色指定・極小操作・残債優先HUDよりも今回のUI仕様を優先する。
表示設定と準備メモはuiState.tsで別保存する。エンジンのbrew.quantityは省略時1、全数の原子的な調合。src/presentation.tsの見込み計算を使い、画面で行動ルールを再実装しない。
